---
title: "Traj-VLN: Learning Pixel-Space Interaction via Autoregressive Trajectory Generation"
title_zh: "Traj-VLN: 通过自回归轨迹生成学习像素空间交互"
authors: "Changfei Fu, Guangcheng Chen, Wenjun Xu, Hong Zhang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10744v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 利用视觉-语言模型的自回归轨迹生成用于视觉语言导航
tldr: 连续环境视觉语言导航(VLN-CE)中，现有方法将视觉嵌入投射到语言空间，但深度感知的3D空间交互对以RGB图像训练的视觉语言模型(VLM)理解困难。本文提出Traj-VLN，通过自回归轨迹生成将导航交互学习转移到2D像素空间：给定指令和历史观测，模型预测像素坐标序列形成轨迹。实验证明，像素空间轨迹监督显著优于离散动作学习，且该模型在有限计算资源和数据下达到SOTA性能。该方法避免了引入VLM预训练中罕见的深度信息，简化了跨场景泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 922, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 920, \"height\": 505, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 633, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1845, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 385, \"label\": \"Table\"}]"
motivation: 克服VLN中深度空间交互对VLM的理解难题，避免引入三维几何信息。
method: 微调VLM在2D像素空间自回归生成轨迹，逐像素坐标预测导航路径。
result: 像素空间轨迹监督超越离散动作学习，以有限资源达到SOTA。
conclusion: 证明像素空间轨迹生成的有效性，为VLM在连续环境导航中提供新范式。
---

## 摘要
受益于大规模预训练数据中嵌入的强大先验知识以及新兴的常识推理能力，大语言模型（LLMs）在许多研究领域展现出前所未有的泛化能力。近年来，通过视觉-语言模型（VLMs）将视觉嵌入投影到语言空间，以实现从仿真到真实以及跨场景的泛化，已成为连续环境下的视觉-语言导航（VLN-CE）领域中一种主流范式。VLN要求具身智能体遵循自然语言指令，在未见过的环境中进行导航。我们强调，VLN任务可以分解为一系列子任务，每个子任务对应一个与环境的3D空间交互过程，例如指令“走到沙发尽头然后左转”所描述的那样。然而，这种涉及沿深度感知方向进入图像的3D空间交互，对VLMs来说却是难以理解的，因为它们主要在RGB图像的对话数据上进行训练。与引入深度或3D几何信息（这些信息在VLMs预训练中很少出现）不同，我们提出了一种替代方法：通过自回归轨迹生成，直接在2D像素空间中微调VLMs以学习导航交互。给定语言指令和历史观测，我们的模型顺序预测一系列像素坐标，从当前观测的底部中心绘制一条轨迹。虽然先前的工作已经证明像素目标监督优于离散动作的学习，但我们的实验进一步验证了像素空间轨迹的监督显著提升了VLN性能。此外，我们展示了我们的旗舰模型在相对有限的计算资源和训练数据下达到了最先进水平的性能。

## Abstract
Benefiting from the powerful priors embedded in large-scale pre-training data and the emerging commonsense reasoning ability, large language models (LLMs) have shown unprecedented generalization capabilities in many research fields. Recently, projecting visual embeddings into the language space via vision-language models (VLMs) to achieve sim-toreal and cross-scene generalization has become a prevailing paradigm in the field of Vision-and-Language Navigation in Continuous Environments (VLN-CE). VLN requires an embodied agent to navigate through unseen environments following natural linguistic instructions. We emphasize that a VLN task can be decomposed into a sequence of sub-tasks, each corresponding to a process of 3D spatial interaction with the environments described by instructions such as "walk to the end of the sofa and turn left." However, such spatial interactions involving moving into the image along the direction of depth sensing are puzzling for VLMs as they were predominantly trained on conversations with RGB images. Rather than incorporating depth or 3D geometric information-which VLMs rarely encounter during pretrainingwe propose an alternative approach: fine-tuning VLMs to learn navigation interactions directly in 2D pixel space through autoregressive trajectory generation. Given a linguistic instruction and historical observations, our model sequentially predicts a series of pixel coordinates, drawing a trajectory from the bottom center of the current observation. While prior work has proved that pixel-goal supervision outperforms learning of discrete actions, our experiments further verify that the supervision of pixel-space trajectory significantly enhances VLN performance. Moreover, we demonstrate that our flagship model achieves state-of-the-art level performance with relatively limited computational resources and training data.

---

## 论文详细总结（自动生成）

# Traj-VLN: 通过自回归轨迹生成学习像素空间交互——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究领域**：连续环境下的视觉-语言导航（VLN-CE），要求智能体依据自然语言指令在未知环境中导航。
- **核心问题**：现有方法主要依赖视觉-语言模型（VLM）将视觉信息投射到语言空间进行推理。然而，VLM 的预训练数据几乎全部为 2D RGB 图像对话，缺乏对 3D 深度感知空间交互（如“走到沙发尽头然后左转”）的理解能力。直接让 VLM 规划 3D 轨迹或预测离散动作存在分布不匹配，导致性能瓶颈。
- **论文主张**：将 VLN 任务重新定义为 **2D 像素空间内的交互过程**，通过让 VLM 学习在图像上自回归绘制“轨迹”（一系列像素坐标）来模拟导航动作，从而避免显式引入深度或 3D 几何信息。核心动机是：像素空间与 VLM 预训练数据分布一致，轨迹监督比单纯像素目标监督能提供更丰富的过程信息，可作为链式思考（Chain-of-Thought, CoT）提升预测质量。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 构建双阶段对话框架：第一阶段 VLM 输出旋转动作（←/→）或停止；若输出“↓”，则进入第二阶段，模型输入当前俯视图像，并**自回归生成一串归一化像素坐标**（`[u1,v1] [u2,v2] ... [uk,vk]`），从图像底部中心开始绘制轨迹。
- 通过 **交叉熵损失** 直接微调 VLM，使其学会将语言指令和历史观测映射到连续的 2D 路径点序列。

### 关键技术细节
1. **模型架构**：基于 Qwen3-VL-8B（公开通用 VLM），**冻结视觉编码器**，微调 LLM 和投影层。
2. **输入处理**：
   - 历史观测：均匀采样 N=8 帧，统一缩放到 384×384。
   - 当前观测：保持 640×480。
   - 俯视图：当模型输出“↓”时，机器人低头 30° 捕获俯视图（640×480）。
3. **监督数据生成**：
   - 从 R2R-CE、RxR-CE、ScaleVLN 数据集中以 4 步间隔采样 107.5K 条轨迹，共 1.75M 样本。
   - 将未来 3D 位姿投影到当前俯视图的像素平面，并**过滤被遮挡或重复的点**（利用深度图）。
   - 保留的像素坐标序列格式化为文本字符串，作为 VLM 的输出目标。
4. **推理与执行**：
   - 当模型输出序列后，利用对应深度图将像素坐标转换为机器人坐标系下的 3D 路径点。
   - 机器人依次向最近点移动，直至轨迹执行完毕，然后重启对话。

### 公式/算法流程（文字说明）
- 3D 位姿 → 像素投影：使用相机内参 K 和外参 Tc2b，将机器人基座位置投影到图像平面，得到 2D 坐标 (u,v)。
- 文本化：坐标归一化后拼接为 `[u1,v1] [u2,v2] ...` 作为监督。
- 训练时，VLM 在第二阶段的自回归生成完全依赖交叉熵损失。

## 3. 实验设计
### 数据集与场景
- **基准数据集**：R2R-CE（约 10.7K 轨迹，1839 条验证未见轨迹）和 RxR-CE（约 19.5K 轨迹，3669 条验证未见轨迹）。
- **补充数据**：ScaleVLN 子集（约 77.3K 轨迹，1.14M 样本），用于旗舰模型训练。
- **评估指标**：NE（导航误差）、OS（成功率上限）、SR（成功率）、SPL（成功路径长度加权）、nDTW（归一化动态时间规整）。

### 对比方法
- **非 VLM 方法**：Sim2Real、AO-Planner、NavMorph。
- **VLM 方法**：StreamVLN、NaVid、NaVid-4D、NaVILA、JanusVLN、InternVLA-N1、Goal2Pixel。
- **消融对比**：在相同配置下（Qwen2.5-VL-7B，LoRA rank=32，冻结视觉编码器），仅改变监督信号：像素目标（InternVLA-N1-S2） vs 轨迹监督（Traj-VLN）。

## 4. 资源与算力
- **旗舰模型（Traj-VLN）**：
  - 硬件：8 个 Ascend 910C NPU。
  - 训练时间：约 91 小时（总计 728 NPU 小时）。
  - 批次大小 128，16998 步，1 epoch，学习率 2e-5，冻结视觉编码器（节省约 62.5% 时间）。
- **消融对比实验（Qwen2.5-VL-7B LoRA）**：
  - 硬件：4 个 Nvidia 4090 GPU（48GB）。
  - 训练时间：约 171 小时（274,266 步，每步 ~23 秒）。
  - 相同数据量 1.75M 样本，仅监督信号不同。

## 5. 实验数量与充分性
- **主要实验**：在两个标准 benchmark（R2R-CE、RxR-CE）上对比 10 种以上方法（表 I），结果全面。
- **消融实验 1**：表 II 严格控制变量（相同骨干、数据、训练配置），仅改变监督信号（像素目标 vs 轨迹监督），验证轨迹监督的优势。
- **消融实验 2**：图 4 分析每轮执行不同数量路径点（单点、多点、最终点作为像素目标）对 SR/OS 的影响，揭示执行步数与性能的 trade-off。
- **链式思维验证**：将轨迹最终点作为像素目标进行评估（Final-Pixel Evaluation），证明自回归生成过程提升了像素目标预测质量。
- **充分性评价**：实验设计较为严谨，对比方法覆盖当前主流，控制变量消融清晰。但缺少真实机器人部署实验，且未对更复杂环境（如动态障碍）进行评估。

## 6. 论文的主要结论与发现
- **性能优势**：旗舰 Traj-VLN（Qwen3-VL-8B）在 R2R-CE 上达到 SR 58.4（超过 InternVLA-N1 3 点），在 RxR-CE 上 SR 52.6（超过 InternVLA-N1 2.9 点），且训练数据更少（1.75M vs 4.72M）。
- **轨迹监督 > 像素目标监督**：在相同配置下（表 II），轨迹监督在 R2R-CE 上 SR 提升 4%，在 RxR-CE 上提升 3.29%。
- **轨迹生成可作为 CoT**：将最终像素作为像素目标评估（Final-Pixel 设置），Traj-VLN 仍优于直接像素目标监督，说明自回归序列生成为最终目标提供了中间推理过程。
- **资源效率**：冻结视觉编码器+轨迹监督可在有限算力下达到 SOTA，无需引入 3D 信息。

## 7. 优点
- **方法创新**：首次提出用 2D 像素轨迹作为 VLN 的监督信号，完美适配 VLM 的预训练分布，避免对深度/3D 信息的依赖和额外对齐模块。
- **实用性**：自回归生成 + 深度图反投影的 pipeline 简单，易于实现和复现。
- **实验扎实**：严格控制变量的消融实验（表 II、图 4）有力证明了核心假设，并量化了执行步数的影响。
- **高效训练**：冻结视觉编码器大幅降低计算成本，同时保持高性能。

## 8. 不足与局限
- **依赖深度图**：虽然监督在 2D 空间，但推理时仍需深度图将像素轨迹转为 3D 动作，这意味着传感器配置未简化。
- **仿真局限**：所有实验在 Habitat 仿真器中进行，未验证在真实机器人上的 sim-to-real 能力。
- **动态场景缺失**：假设环境静止，未考虑动态障碍物或行人。
- **执行步数权衡**：图 4 显示执行更少路径点（更多推理）提升 SR 但降低 OS，最佳配置需手动选择。
- **数据依赖**：尽管训练数据量相对较小，但仍需 1.75M 预生成样本，且 ScaleVLN 数据集为额外收集，泛化性可能受限于数据多样性。
- **仅评估两个 benchmark**：未在更多 VLN-CE 基准（如 R2R-Habitat 完整版）或零样本场景下验证。

（完）

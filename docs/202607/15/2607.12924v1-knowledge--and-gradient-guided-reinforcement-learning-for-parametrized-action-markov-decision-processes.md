---
title: Knowledge- and Gradient-Guided Reinforcement Learning for Parametrized Action Markov Decision Processes
title_zh: 用于参数化动作马尔可夫决策过程的知识与梯度引导强化学习
authors: "Jonas Ehrhardt, René Heesch, Oliver Niggemann"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12924v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 参数化动作MDP的强化学习
tldr: 在参数化动作马尔可夫决策过程（PAMDP）中，强化学习样本效率低且未充分利用领域知识。本文提出知识梯度引导强化学习（KGRL），利用Datalog知识库剪枝不可行动作并约束参数空间，再通过梯度优化精调参数。实验表明KGRL在样本效率和回合回报上均超越现有基线，同时提供局部可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12924v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12924v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 690, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12924v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1777, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12924v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 768, \"height\": 577, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12924v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 1982, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12924v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 727, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12924v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1811, \"height\": 708, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12924v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 340, \"label\": \"Table\"}]"
motivation: PAMDP中现有强化学习方法参数估计低效，而可用的领域知识未被充分利用，导致样本效率低下。
method: KGRL使用Datalog知识库导出适用动作与可行参数，裁剪动作空间并约束参数，再通过梯度迭代优化参数。
result: KGRL在多个PAMDP环境中样本效率和回合回报均优于当前最先进强化学习基线。
conclusion: KGRL有效融合符号知识引导探索，提升训练效率和决策可靠性，并具备可解释性。
---

## 摘要
本文研究了参数化动作马尔可夫决策过程（PAMDP）中的强化学习，其中每个决策由一个符号动作和数值参数组成。在这种设置下，强化学习算法通常使用一次性估计器确定参数，导致训练样本效率低下。尽管大多数PAMDP环境中存在显式但不完整的知识（如规则、安全约束或专家启发式），但这些知识很少被直接用于提高强化学习智能体的训练样本效率。我们填补了这一空白，提出了新颖的神经符号知识与梯度引导强化学习（KGRL）算法。KGRL利用Datalog知识库中的领域知识，推导出给定状态下的适用动作集和可行参数，从而从决策空间中剪除不适用动作，并约束剩余动作的参数空间。然后，我们使用基于梯度的参数细化循环，在智能体训练和部署期间估计最优参数。通过记录轨迹中激活的规则，KGRL还为动作剪枝和参数约束提供了局部程序化解释。总体而言，KGRL将智能体的探索和部署引导至可行且约束感知的决策，同时提高了训练期间的样本效率。在PAMDP的样本效率和回合回报方面，KGRL均优于最先进的强化学习基线。

## Abstract
In this paper, we study Reinforcement Learning in Parametrized Action Markov Decision Processes (PAMDP), where each decision consists of a symbolic action and numerical parameters. In such settings Reinforcement Learning algorithms typically determine parameters with one-shot estimators, which makes their training sample inefficient. Though in most PAMDP environments explicit but incomplete knowledge (e.g., rules, safety constraints, or expert heuristics) is available, it is rarely directly used to increase the sample-efficiency of training Reinforcement Learning agents. We step into this gap and propose our novel Neuro-Symbolic Knowledge- and Gradient-Guided Reinforcement Learning (KGRL) algorithm. KGRL uses domain knowledge in a Datalog knowledge base to derive the set of applicable actions and feasible parameters for a given state. This allows it to prune non-applicable actions from the decision-space and constrain the parameter spaces of the remaining actions. We then use a gradient-based parameter refinement loop to estimate the optimal parameters during training and deployment of the agent. By recording activated rules along the trajectory, KGRL additionally provides local procedural explanations on the pruning of actions and constraining of parameters. Overall, KGRL guides the agent's exploration and deployment toward feasible and constraint-aware decisions, while increasing sample efficiency during training. KGRL outperforms state-of-the-art RL baselines for PAMDPs in both, sample efficiency and episodic return.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题背景**：在参数化动作马尔可夫决策过程（PAMDP）中，每个决策包含一个符号动作和一个连续数值参数。现有强化学习（RL）算法通常使用一次性估计器（one-shot estimator）来确定参数，导致训练样本效率低下。
- **核心动机**：尽管大多数PAMDP环境下存在显式但不完整的领域知识（如规则、安全约束、专家启发式），但这些知识很少被直接利用来提升RL智能体的训练效率。论文旨在回答：**能否将不完整的符号知识集成到RL的训练和部署中，在强制执行已知约束的同时，让学习算法优化符号无法指定的部分？**
- **整体含义**：提出一个神经符号方法，利用符号知识库剪枝不可行动作并约束参数空间，再通过梯度优化精调参数，从而在提高样本效率的同时保持决策的可行性和可解释性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：在DQN框架基础上，集成两个关键机制：
  1. **Datalog知识库（K）**：在每个状态s，通过抽象函数α(s)将连续状态转换为符号事实，与规则集K一起推导出**适用动作集**Ā(s)和**可行参数区域**{Ψ̄_a(s)}。
  2. **梯度引导参数精化（ParamOpt）**：对每个适用动作a，使用投影梯度上升法沿Qθ对参数ψ的梯度，在可行参数区域内迭代优化参数，直至收敛或达到最大迭代次数。
- **关键技术细节**：
  - **知识约束PAMDP**：形式化定义知识约束PAMDP，要求策略π仅在Ā(s)和Ψ̄_a(s)内选择动作-参数对。
  - **Algorithm 1 (KGRL训练循环)**：
    - 初始化双DQN（在线网络Qθ和目标网络Qθ⁻）、重放缓冲区D。
    - 每个时间步：评估知识库得到Ā(s)和可行参数；以ε-贪心选择探索或利用。
    - 利用时：对每个适用动作调用ParamOpt；选择Q值最高的动作-参数对执行；存储经验到D。
    - 定期采样小批量，计算目标y = r + γ(1-d')·max_{a'∈Ā(s')} max_{ψ'∈Ψ̄_a'(s')} Qθ⁻(s', a', ψ')，更新在线网络。
  - **Algorithm 2 (ParamOpt)**：
    - 输入：状态s、动作a、可行参数区域Ψ̄_a(s)、DQN Qθ。
    - 初始化参数猜测ψ̂（零、随机或专家启发式）。
    - 循环至多Umax步：计算梯度g = ∇_ψ Qθ(s,a,ψ̂)；投影梯度上升：ψ̂ ← Proj_{Ψ̄_a(s)}(ψ̂ + ζg)；若梯度范数小于阈值ξ则提前停止。
    - 返回精化后的参数ψ̃*。
- **可解释性**：通过记录每步激活的规则，生成局部程序化解释追踪（explanation trace），说明为何某些动作被剪枝、参数为何被约束。

## 3. 实验设计：数据集/场景、benchmark、对比方法
- **评估域**（共4类6个场景）：
  - **HardMoveX**（n=4,8,10）：2D导航，代理需到达目标，动作是方向+幅度。规则剪枝指向远离目标方向的动作，并根据距离限制幅度。
  - **CatchPoint**：2D追逐任务，动作有移动（ψ∈[-1,1]）和抓捕（ψ∈[0,1]）。规则剪枝：到达目标时不允许移动，不可能抓住时不允许抓捕。
  - **HardGoal**：简化足球射门，10个射门动作（a0-a9）参数ψ∈[-1,1]，守门员移动。规则根据守门员位置剪枝低/高半区动作及被阻挡动作。
  - **Platform**：平台跨越任务，动作有跑（ψ∈[0,30]）、跳（ψ∈[0,720]）、跃（ψ∈[0,430]）。规则剪枝靠近缺口或对手的危险动作，并根据环境特征约束参数。
- **基准方法**：QPAMDP、PA-DDPG、P-DQN、MP-DQN、HyAR（共5种）。
- **训练配置**：固定预算500,000步，最大计算墙时12小时，8个不同随机种子。每1,000步在16个不同初始化问题上评估一次。

## 4. 资源与算力
- 论文提及使用HPC集群**HSUper**（项目hpc.bw），但**未明确说明GPU型号、数量、具体训练时长**。仅提到训练预算为500k环境步，最大计算墙时12小时。未详细披露算力资源。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验：在6个域（HardMove4/8/10, CatchPoint, HardGoal, Platform）上对比5种基线，每个域8个种子，共48组×5=240个训练运行。
  - 消融实验：对KGRL进行6种消融（训练/评估阶段分别去除知识库或梯度引导），同样8个种子，共6×8×6=288个额外运行。
  - 可解释性分析：统计知识库利用率（K-utilization）和动作剪枝率（A-pruning）。
- **充分性**：
  - 覆盖了多种参数化动作空间类型（导航、追逐、竞技、平台），不同动作数和规则复杂度。
  - 使用AULC（学习曲线下面积）综合评价性能和收敛速度。
  - 遵循了Vranješ等2024的实证机器学习设计原则。
  - 消融实验系统性地分离了知识库和梯度引导的影响。
- **客观性**：报告了均值和标准差，种子数8较为合理，但未进行统计显著性检验（如t检验或Mann-Whitney U）。

## 6. 论文的主要结论与发现
- **性能优越**：KGRL在所有域上均取得最高AULC，且收敛更快、方差更低（见图3和表1）。
- **知识库作用**：去除知识库（特别是在评估阶段）导致性能严重下降，说明知识库主要在部署时提供约束，但训练时影响较小甚至略有干扰。
- **梯度引导作用**：ParamOpt在评估时提升性能，但在训练时引入噪声导致负效果（HardMove域尤为明显）。因此建议仅在部署时使用ParamOpt，或采用课程学习调度。
- **可解释性**：知识库利用率高（多数域接近1.0），动作剪枝率较高（HardMove约49%，其他约30-34%），说明规则频繁用于简化决策空间，并可记录规则激活作为局部解释。

## 7. 优点
- **方法新颖性**：首次将符号知识库与梯度参数优化结合用于PAMDP，填补了神经符号RL在参数化动作空间中的空白。
- **样本效率高**：利用先验知识避免无效探索，加速收敛。
- **可解释性强**：每步决策均可追溯规则激活，提供局部程序化解释，适用于安全关键应用。
- **设计合理**：采用默认适用性方案（default applicability），处理不完整知识；投影梯度上升保证参数不越界。
- **消融实验全面**：分别测试了知识库和梯度引导在训练/部署各阶段的贡献，揭示了各自的最佳使用时机。
- **遵循科学规范**：提及采用可复现性设计原则，开源代码。

## 8. 不足与局限
- **对错误知识敏感**：硬剪枝和硬约束可能因错误规则而排除最优动作或参数，文中仅建议自适应机制，未深入解决。
- **梯度优化不保证全局最优**：ParamOpt是非凸优化，可能陷入局部最优，未尝试多起点或集成等策略。
- **解释限于局部**：只能提供单步解释，未实现全局行为模式聚合（如频繁触发相同规则的总结）。
- **知识库和抽象函数手工定义**：依赖专家手工规则，限制了实用性；未实验学习型抽象或规则学习方法。
- **未进行统计显著性检验**：虽报告均值和标准差，但未使用假设检验证明差异显著。
- **计算成本未充分报告**：未比较不同方法的实际运行时间或GPU资源消耗。
- **环境单一均为模拟**：所有域为2D简化物理模拟，未在真实机器人或高维环境中验证。
- **消融实验中的矛盾现象**：CatchPoint中去除梯度引导反而略优，论文未深入解释。

（完）

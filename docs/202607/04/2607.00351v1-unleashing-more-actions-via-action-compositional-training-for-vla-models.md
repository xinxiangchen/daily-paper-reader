---
title: Unleashing More Actions via Action Compositional Training for VLA Models
title_zh: 通过动作组合训练释放VLA模型的更多动作能力
authors: "Kai Peng, Jie Lu, Xiaojiang Peng"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00351v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 面向视觉-语言-动作模型的行动组合训练
tldr: VLA模型在机器人操作中因过拟合特定行为而泛化差，即使新任务仅需组合已知子技能。ACT-VLA通过动作组合训练，利用模型潜在任务表示从现有任务自动合成物理可行的新演示，无需额外数据。在模拟任务中，该方法显著提升了策略成功率，有效缓解了分布外泛化问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00351v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 1027, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 528, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00351v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1512, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00351v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 928, \"height\": 244, \"label\": \"Table\"}]"
motivation: 标准VLA训练过拟合特定行为模式，无法泛化到子技能新组合场景，且扩展高质量数据成本高昂。
method: 提出ACT-VLA离线数据增强框架，利用模型潜在任务表示从现有任务合成新颖且物理可行的演示用于训练。
result: 在挑战性模拟操作任务中，ACT-VLA训练的策略成功率显著高于基线模型。
conclusion: 无需额外数据，自动合成演示能有效缓解过拟合，扩展VLA模型泛化能力，提供高效可扩展的路径。
---

## 摘要
视觉-语言-动作模型在机器人操作中表现出色，这得益于示范数据的规模和多样性。然而，标准训练范式常常导致VLA模型严重过拟合于特定的行为模式，即使面对仅需相同子技能的新颖组合的场景，也无法泛化到分布外的情况。虽然扩展数据集可以缓解这种过拟合，但获取高质量机器人数据仍然众所周知地劳动密集且成本高昂。为了在不依赖昂贵的人工遥操作的情况下解决这一困境，并真正释放更多动作——即让VLA模型能够将已知子技能组合成远超原始示范的可执行行为集合——我们提出了ACT-VLA（面向VLA模型的动作组合训练），这是一种离线数据增强框架，利用模型的潜在任务表示，直接从现有任务中合成新颖且物理有效的示范用于策略训练。通过消除额外的手动数据收集，我们的方法自动扩展训练分布并减轻过拟合。我们在模拟中评估了具有挑战性的操作任务。实验表明，由于原始分布过拟合，基线VLA模型泛化能力较差，而使用我们合成数据训练的策略实现了显著更高的成功率，验证了利用现有任务进行自动化示范合成为拓宽VLA泛化能力提供了一条高效、可扩展且数据经济的路径。

## Abstract
Vision-Language-Action models excel at robotic manipulation, driven by the scale and diversity of demonstration data. However, standard training paradigms often cause VLA models to severely overfit to specific behavioral patterns, rendering them unable to generalize to out-of-distribution scenarios even when those scenarios merely require novel combinations of identical sub-skills. While expanding datasets can mitigate this overfitting, acquiring high-quality robot data remains notoriously labor-intensive and cost-prohibitive. To resolve this impasse without expensive human teleoperation and to truly unleash more actions,i.e., enable VLA models to compose known sub-skills into a much broader set of executable behaviors beyond the original demonstrations-we propose ACT-VLA (Action Compositional Training for VLA Models), an offline data augmentation framework that leverages the model's latent task representations to synthesize novel, physically valid demonstrations directly from existing tasks for policy training. By eliminating additional manual data collection, our method automatically expands the training distribution and mitigates overfitting. We evaluate our approach on challenging manipulation tasks in simulation. Experiments demonstrate that while baseline VLA models generalize poorly due to original distribution overfitting, policies trained with our synthesized data achieve substantially higher success rates, validating that leveraging existing tasks for automated demonstration synthesis provides an effective, scalable, and data-efficient route to broadening VLA generalization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言-动作模型（VLA）虽然在大规模模仿学习下表现出色，但存在严重的**分布过拟合**问题。即模型只能执行训练数据中出现的特定行为模式，无法泛化到那些仅需重新组合已学子技能的新任务（称为组合泛化）。例如，训练中模型只见过“将立方体放在矩形托盘上”和“将杯子放在圆形盘子上”，但测试时要求“将杯子放在矩形托盘上”时，模型可能失败。由于可能的组合数量呈组合爆炸增长，通过人工收集所有组合的演示数据是不现实的。

- **研究动机**：现有方法如推理时的文本潜在插值（TLI）可以在不改变模型的情况下实现组合行为，但需要每个回合进行干预，增加了部署开销。作者希望**将组合轨迹合成从推理时转移到训练时**，通过离线数据增强让模型自然地学到组合技能，从而消除推理时的额外计算。

- **整体含义**：提出一种名为**ACT-VLA**的离线数据增强框架，利用已有演示轨迹自动生成缺失的组合演示数据，无需额外的人工遥操作。实验证明该方法显著提升了VLA模型在分布外组合任务上的成功率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用预训练VLA模型中编码的潜在组合结构，通过**文本潜在插值（TLI）** 在离线阶段生成新颖组合任务的演示轨迹，并对这些轨迹进行质量过滤和格式处理，然后加入到标准的行为克隆训练中，使模型直接将组合能力内化到权重中。

- **关键技术细节**：
  1. **文本潜在插值（TLI）** ：借鉴Li等人[12]的方法。Transformer型VLA模型在文本令牌的隐藏状态中编码了任务语义。通过平均多个演示回合的隐藏状态，得到任务特定的文本潜在表示 **T**。对于两个基础任务A和B，在每一步i，将文本隐藏状态修改为：
     \[
     h_T(i) \leftarrow h_T(i) + (1-\alpha)T_1 + \alpha T_2 - \left[(1-\alpha)T_2 + \alpha T_1\right]
     \]
     其中 \(\alpha = i/\lambda\) 线性地从0到1变化。这样，初期倾向于任务A，后期逐渐过渡到任务B，生成连贯的组合轨迹。
  2. **推理时到训练时的转变**：作者将TLI从推理时的干预改为离线数据生成引擎。并针对训练数据的使用，提出了三个关键设计：
     - **指令设计（Instruction Design）**：生成的轨迹需要对应的自然语言指令。作者将原始任务指令的文本成分拆分重组，形成描述新组合任务的指令。
     - **质量控制（Quality Control）**：仅保留在模拟器中成功完成组合任务（满足LIBERO任务完成标准）的轨迹，并限制最大步长，剔除低质量或失败的轨迹，防止错误关联污染训练集。
     - **训练动态（Training Dynamics）**：保持原始数据与合成数据在套件级别的数量平衡，防止合成分布过度主导导致灾难性遗忘。
  3. **数据记录与处理**：对于成功生成的轨迹，记录每步的多视角RGB图像、机器人本体状态、执行动作以及组合语言指令。格式与原始数据集严格匹配，不需修改训练流程。
  4. **增强训练**：在合成数据集 \(D_{\text{syn}}\) 上使用标准行为克隆损失和超参数训练VLA模型，不做架构修改。

### 3. 实验设计：使用了哪些数据集/场景，benchmark，对比了哪些方法

- **基准**：LIBERO仿真套件，包含四个标准套件（Spatial、Object、Goal、Long，各10个任务）和两个分布外（OOD）套件（Spatial-OOD、Goal-OOD），后者由作者根据[12]的方法构造，每套约3-4个组合任务（实际表格中OOD套件任务数较少，但论文未明确具体数量，只说明匹配原始套件的数据规模）。
- **对比方法**：
  - **基线模型**：UniVLA、OpenVLA-OFT、GR00T-N1、π0.5（所有模型均在原始LIBERO演示上训练）。
  - **推理时TLI方法**：π0在推理时使用文本潜在插值（π†₀ [12]）。
  - **消融实验**：在π0骨干上比较：原始数据仅训练、原始+合成数据训练、推理时TLI。
- **评估协议**：每个任务运行3个随机种子，每种子50个回合（每任务150回合）。报告每个套件内所有任务的平均成功率（%）。
- **结果**：
  - ACT-VLA（使用π0.5骨干）在Spatial-OOD上达到88.2%（基线π0.5为35.5%），在Goal-OOD上达到95.6%（基线为46.6%），绝对提升分别为52.7和49.0个百分点。
  - 六套件未加权均值AVG达95.6%，超过所有基线（包括推理时TLI的90.4%）。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU型号、数量、训练时长等）。若未明确说明，也请指出这一点

- **论文中未明确说明**所使用的GPU型号、数量和训练时长。仅提及所有模型在相同设置下训练30k步（30k steps），但未提供具体硬件信息。因此无法总结算力细节。

### 5. 实验数量与充分性：大概做了多少组实验，是否充分、客观、公平

- **实验数量**：
  - 主实验：6个套件 × 多个模型（5个基线+自己的方法），每个任务150回合，统计平均成功率。
  - 消融实验：在π0骨干上进行了3组对比（原始数据 vs 推理时TLI vs 原始+合成数据），报告了Spatial和Goal两个OOD套件的结果。
  - 此外还有表格II中的对比，但未做更多超参数或更多基线的消融。
- **充分性评价**：
  - **充分**：覆盖了常见的VLA模型，包括不同规模和方法（UniVLA、OpenVLA等），并且对比了推理时TLI，直接验证了训练时增强的优势。
  - **客观公平**：所有模型在同一训练步数（30k）、相同评估协议下比较，消融实验保持骨干相同，控制了变量。报告的是均值，且使用了未加权平均防止大套件稀释OOD效果。
  - **不足**：只做了组合任务（双技能组合），未测试更长链组合；只在一个仿真基准（LIBERO）上测试，未在真实机器人上验证；未做更多超参数或数据比例的消融。
- **整体充分性**：对于所提方法的验证是充分的，但泛化性（真实场景、多技能链）有待进一步研究。

### 6. 论文的主要结论与发现

- **主要结论**：通过离线数据合成组合演示，可以显著提升VLA模型在分布外组合任务上的泛化能力，且效果优于推理时潜在干预方法。具体来说：
  1. 使用ACT-VLA在Spatial-OOD和Goal-OOD上分别获得52.7和49.0个百分点的提升。
  2. 相比推理时TLI，训练时合成不仅性能更高（88.2% vs 81.0%），还消除了推理时的计算开销。
  3. 该方法无需额外人工遥操作、无需改变模型架构或推理时干预，是一种实用且数据高效的路径。

- **发现**：VLA模型的潜隐表示中已经编码了丰富的组合结构，可以通过插值生成新任务轨迹，并且这些轨迹作为训练数据能有效内化组合能力。

### 7. 优点：方法或实验设计上有哪些亮点

- **方法亮点**：
  1. **数据驱动**：将组合泛化问题转化为数据增强问题，无需修改架构、无需外部规划器或推理时干预，降低了部署复杂度。
  2. **成本低**：完全利用现有演示数据自动合成新数据，避免了昂贵的人工遥操作。
  3. **易于集成**：是“即插即用”的离线框架，可与任何VLA模型和训练流程结合。
  4. **质量控制**：通过模拟器严格筛选成功轨迹，并控制合成数据与原始数据的比例，防止过拟合或灾难性遗忘。

- **实验亮点**：
  1. 对比了多种主流VLA模型（UniVLA、OpenVLA-OFT、GR00T-N1、π0系列），基线丰富。
  2. 设计了消融实验，在相同骨干下对比推理时TLI和训练时合成，直接证明了方法的优势。
  3. 采用未加权平均AVG，避免了因OOD套件任务少而被稀释，评价更公正。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖**：
  - 仅测试了**两技能顺序组合**，未探索更长的技能链或更复杂的组合方式。
  - 所有实验在**仿真**环境（LIBERO）中进行，未在真实机器人上部署，存在sim-to-real gap。
  - 仅使用一个基准套件，未在其他机器人操作基准（如CALVIN、MetaWorld等）上验证。

- **偏差风险**：
  - 合成数据的质量依赖于模拟器任务完成的严格判断；如果模拟器判断标准不完美，可能引入偏差。
  - 合成数据与原始数据的平衡比例仅以套件级别匹配，未精细探索不同比例的影响。
  - 方法依赖预训练VLA的潜藏表示具有良好语义解耦能力；若骨干模型较弱，合成效果可能受限。

- **应用限制**：
  - 对于需要多技能链（大于2）的组合，递归地组织轨迹会带来指数级优化挑战。
  - 本方法假设任务可由子技能线性插值得到，可能不适用于需要非线性突发行为的任务。
  - 需要可重复的仿真环境进行轨迹合成，对于真实世界在线合成可能不现实。

（完）

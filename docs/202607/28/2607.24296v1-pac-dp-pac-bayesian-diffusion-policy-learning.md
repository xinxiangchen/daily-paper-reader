---
title: "PAC-DP: PAC-Bayesian Diffusion Policy Learning"
title_zh: "PAC-DP: 基于PAC-贝叶斯的扩散策略学习"
authors: "Mohammad Hasan Yeganegi, Dian Yu, Andrea Del Prete, Majid Khadiv, Matteo Saveriano"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24296v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: PAC-贝叶斯扩散策略学习用于机器人操作
tldr: 扩散策略在有限数据下泛化不足，本文提出PAC-DP方法，将扩散策略建模为贝叶斯神经网络，利用PAC-Bayes理论导出KL散度正则化的训练目标，在不增加训练时间的前提下提升泛化能力。实验表明，该方法在多个机器人操作基准上显著提高去噪性能和任务成功率，尤其在低数据复杂任务中改进最大，为机器人策略学习提供了理论驱动的框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24296v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1510, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1863, \"height\": 304, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1887, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 919, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24296v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1867, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1870, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 673, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 910, \"height\": 245, \"label\": \"Table\"}]"
motivation: 扩散策略在有限数据下泛化能力有限，缺乏原则性正则化方法。
method: 将扩散策略建模为贝叶斯神经网络，利用PAC-Bayes界导出后验与先验的KL散度正则项。
result: 在多个机器人操作基准上，去噪性能和任务成功率显著提升，低数据场景改进最大。
conclusion: PAC-DP为机器人策略学习提供了理论驱动的正则化框架，兼具高效与强泛化。
---

## 摘要
扩散策略（DPs）能够执行复杂操作任务。然而，DPs通常通过最小化去噪目标进行训练，这在机器人学中常见的有穷数据场景下对泛化能力的控制有限。在本文中，我们提出PAC-DP，一种提升DPs在机器人操作任务中性能的方法。通过将DP建模为贝叶斯神经网络，并定义PAC-贝叶斯泛化界，我们推导出一种新的训练目标，该目标用后验参数分布与先验参数分布之间的KL散度正则化项来增强标准的去噪损失。从理论角度看，我们的方法为正则化DPs的训练提供了原则性方法，且不会显著增加训练时间。从实践角度看，实验结果表明，在多个机器人操作基准测试中，去噪性能提升、变分负对数似然降低，成功率提高。关键的是，在低数据训练场景和复杂任务中观察到最大的改进，使得PAC-DP成为机器人策略学习的理论扎实框架。

## Abstract
Diffusion Policies (DPs) are able to perform complex manipulation tasks. However, DPs are typically trained by minimizing a denoising objective, which provides limited control over generalization in the finite-data regimes common in robotics. In this letter, we propose PAC-DP, an approach that increases the performance of DPs in robotic manipulation tasks. By modeling the DP as a Bayesian neural network, and defining a PAC-Bayes generalization bound, we derive a novel training objective that augments the standard denoising loss with a Kullback-Leibler divergence regularizer between the posterior and prior parameter distributions. From the theoretical perspective, our approach provides a principled approach to regularize the training of DPs without significantly increasing the training time. From the practical point of view, experimental results demonstrate improved denoising performance, lower variational negative log-likelihood, and higher success rates across multiple robotic manipulation benchmarks. Crucially, the largest improvements are observed in low-data training regimes and complex tasks, establishing PAC-DP as a theoretically grounded framework for robot policy learning.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：扩散策略（Diffusion Policies, DPs）在机器人模仿学习中表现优异，但其标准训练目标仅最小化去噪损失，在机器人学常见的有穷数据场景（尤其是低演示数量）下，无法保证良好的泛化能力。已有研究表明，仅优化去噪损失可能导致模型记忆训练样本而非学习真实数据分布，从而限制策略的实际部署效果。
- **研究动机**：现有改进DP泛化的方法多依赖架构设计或扩大数据规模，缺乏对训练目标本身的理论指导。作者希望引入统计学中的PAC-贝叶斯（PAC-Bayesian）学习理论，为DP提供原则性的正则化框架，以提升泛化性能，尤其在数据稀缺时。
- **整体含义**：这项工作将PAC-Bayesian理论与扩散策略结合，提出了一个理论驱动且实用的训练目标，在不显著增加训练时间的前提下，有效提升了去噪精度、生成模型质量以及下游任务成功率。这为机器人策略学习提供了新的理论视角，并展示了在低数据复杂任务中的显著优势。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将扩散策略中的噪声预测网络建模为贝叶斯神经网络（BNN），学习网络参数的后验分布 \( Q(w) \)。利用PAC-Bayesian泛化界，推导出一个包含标准去噪损失和KL散度正则项的训练目标，从而在经验风险最小化和模型复杂度之间取得平衡。
- **关键技术细节**：
  - **概率模型**：先验分布 \( P(w) \) 和后验分布 \( Q(w) \) 均设为因子化高斯分布。后验参数包括均值 \( \mu \) 和标准差 \( \sigma \)，其中 \( \sigma = \log(1 + e^{\rho}) \) 确保非负。
  - **重参数化采样**：训练时通过 \( w = \mu + \sigma \odot \xi,\ \xi \sim \mathcal{N}(0,I) \) 采样，使梯度可反向传播。
  - **PAC-Bayes训练目标**：
    \[
    L_{\text{PAC-DP}} = \hat{R}(Q) + \sqrt{\frac{2 \hat{R}(Q) (\lambda D_{\text{KL}}(Q\|P) + \ln\frac{2\sqrt{N}}{\delta})}{N}} + \frac{2 (\lambda D_{\text{KL}}(Q\|P) + \ln\frac{2\sqrt{N}}{\delta})}{N}
    \]
    其中 \( \hat{R}(Q) = \mathbb{E}_{w \sim Q}[L_{\text{DP}}(w)] \) 是经验去噪损失，\( D_{\text{KL}} \) 是KL散度正则项，\( \lambda \) 控制正则化强度，\( N \) 为训练样本数，\( \delta \) 为置信度参数。
  - **算法流程**：
    - **训练**：每步采样 \( (x_0, o) \)、时间步 \( t \)、噪声 \( \epsilon \)；构造 \( x_t \)；从后验采样 \( w \)；计算噪声预测 \( \hat{\epsilon} \)；计算 \( L_{\text{PAC-DP}} \)；更新 \( \mu, \rho \)。
    - **推理**：可选择使用后验均值（确定性）或采样随机参数；采用DDIM加速采样，仅用10个去噪步。

## 3. 实验设计：数据集/场景、基准、对比方法
- **数据集与场景**：
  - **模拟环境**：使用DP基准套件中的PushT任务和Robomimic套件中的五个任务（Lift, Can, Square, Transport, ToolHang）。所有任务使用状态观测和成功专家演示。
  - **真实机器人**：Franka Emika Panda机器人，执行Lift, Can, Square, ToolHang四个任务，每个任务5条轨迹×10次执行共50次测试。
- **基准（Benchmark）**：采用与Chi等人[2]相同的设置，包括任务配置和评估协议。
- **对比方法**：
  - **DP**：标准扩散策略，使用确定性网络和标准去噪损失。
  - **BDP**（Bayesian DP，消融基线）：使用贝叶斯神经网络进行噪声预测，但仅优化标准去噪损失 \( \hat{R}(Q) \)，不含PAC-Bayesian正则化。

## 4. 资源与算力
- 文中**未明确说明**使用的GPU型号、数量或训练时长。仅提及训练步数：PushT训练10⁶步，Robomimic任务中Lift/Can/Square训练2.5×10⁵步，Transport/ToolHang训练5×10⁵步。作者声称PAC-DP训练时间**不会显著增加**，但未提供具体耗时对比数据。

## 5. 实验数量与充分性
- **实验数量**：
  - 模拟实验覆盖6个任务（PushT + 5 Robomimic），每个任务使用多个演示数量（20, 40, 80, 200），共约24组主实验。
  - 消融实验包括：超参数敏感性（λ和ρ的8种组合）、贝叶斯网络架构对比（全BN vs 仅去噪网络为BN）、低数据场景对比。
  - 每个实验报告3个种子的均值和标准差，最后10个checkpoint平均，确保稳定性。
  - 真实机器人实验共200次执行。
- **充分性与公平性**：
  - 实验设计全面，覆盖了多种任务复杂度和数据规模，消融实验分离了贝叶斯参数化和正则化的贡献。
  - 对比方法均为合理基线，评估指标多样（NPE, NLL, SR）。
  - 但未提供统计显著性检验（如t检验），且未与近期的其他正则化方法（如数据增强、权重衰减）进行对比。低数据实验未覆盖所有任务（Transport/ToolHang仅200演示）。总体而言实验较充分，但轻微不足在于未系统对比最先进的正则化技术。

## 6. 论文的主要结论与发现
- **主要结论**：PAC-DP在所有任务上均降低了验证集噪声预测误差（NPE）和变分负对数似然（NLL），表明其学习到了更准确的去噪模型和更紧的似然界。在任务成功率方面，尤其在**低数据场景**（20/40演示）和**复杂任务**（如ToolHang）上提升显著。例如PushT在200演示下成功率从86.1%提升至93.7%，ToolHang从40.1%提升至73.8%。简单任务（Lift, Can）三者均接近完美，改进空间有限。
- **关键发现**：性能提升来自PAC-Bayesian正则化而非贝叶斯参数化本身（BDP仅略优于DP，而PAC-DP大幅超越两者）。贝叶斯网络架构中，仅将去噪网络设为贝叶斯而保持条件网络确定性，效果远优于全贝叶斯网络（全BN在ToolHang上成功率最高仅27.8%，而PAC-DP可达85.8%）。
- **实际部署**：在真实Franka机器人上，200次执行全部成功，验证了策略的可迁移性。

## 7. 优点：方法或实验设计上的亮点
- **理论驱动**：直接从PAC-Bayesian泛化界导出可训练目标，具有严格的理论保证，不同于多数经验性正则化方法。
- **通用性**：不改变DP的架构或采样过程，可直接替换原有损失函数，易于集成。
- **低数据场景优势**：在演示数量有限时效果显著，这符合机器人实际需求（数据采集昂贵）。
- **消融设计合理**：通过BDP基线干净地分离了贝叶斯参数化与正则化的贡献，实验可解释性强。
- **真实机器人验证**：在物理系统上证明了方法的实用性和鲁棒性，增加了结果的可信度。

## 8. 不足与局限
- **简单任务提升有限**：在Lift、Can等简单任务上，所有方法均接近100%成功率，PAC-DP的优势无法充分体现，且NPE降低并未始终转化为成功率提升（如Square, Transport）。
- **调参依赖**：超参数λ和ρ需要针对任务调整（文中λ范围10⁻⁵~10⁻⁶，ρ范围-3.5~-5.0），尽管文中表明平均性能接近最优，但仍需调参工作。
- **缺乏与最新正则化方法的比较**：未与权重衰减、数据增强、dropout等常见正则化技术对比，也未与近期专门针对扩散模型泛化的方法（如隐式正则化）对比，削弱了声称的“原则性”优势的实证说服力。
- **真实实验规模较小**：仅4个任务各50次执行，且缺乏不同演示数量或随机初始条件的系统评估，统计显著性有限。
- **计算成本未量化**：虽然声称不显著增加训练时间，但未提供实际训练时间、内存占用或推理速度的对比数据，无法验证效率声称。
- **损失函数的有界性假设**：PAC-Bayes界要求有界损失函数，而文中采用未截断的\( L_2 \)去噪损失（理论上是无界的），仅以“实践中损失很快小于1”为理由，缺乏理论严谨性。

（完）

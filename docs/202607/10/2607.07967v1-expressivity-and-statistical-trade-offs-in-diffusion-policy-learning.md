---
title: Expressivity and Statistical Trade-offs in Diffusion Policy Learning
title_zh: 扩散策略学习中的表达能力与统计权衡
authors: "Viet Vu, Renyuan Xu, Jiacheng Zhang, Yufei Zhang"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07967v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 扩散策略在强化学习中的理论分析
tldr: "扩散策略在强化学习中能建模复杂多模态动作分布，但其表达性与统计代价的权衡尚不明确。本文发现漂移的Lipschitz预算K是核心控制量：K越大，策略逼近最优确定性策略的误差越小（阶1/K），但统计复杂度越高。对于神经网络参数化，平衡两者得到有限样本误差阶O~(n^{-2/(m+6)})，而单边耗散类可改进至O~(n^{-2/(m+4)})。理论表明应根据样本量选择K，从而指导网络架构设计。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07967v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1681, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07967v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1693, \"height\": 463, \"label\": \"Figure\"}]"
motivation: 扩散策略在实践中表达性强，但驱动因素及其有限样本下的统计代价缺乏理论解释。
method: 将扩散策略表达性归结为漂移的Lipschitz预算K，并分析其近似误差与统计复杂度的权衡。
result: 证明了近似误差上下界与K的关系，并推导了神经网络参数化下的有限样本性能阶。
conclusion: 应根据样本量选择K，再设计固定Lipschitz系数的网络，以平衡表达性与统计效率。
---

## 摘要
基于扩散的策略最近成为强化学习中强大的策略参数化方法，将状态条件动作分布表示为具有参数化漂移的扩散过程的终端法则。这种终端法则表示在实践中显示出显著的表达灵活性，使扩散策略能够建模复杂、多模态且高度非高斯的动作分布；然而，尚不清楚是什么在数学上驱动了这种表达能力，以及当从有限数据中学习策略时如何充分利用它。在本文中，我们确定漂移Lipschitz预算$K$是控制扩散策略表达能力和统计行为的中心量。我们通过近似来量化表达能力：具有$K$-Lipschitz漂移的扩散策略可以集中在最优确定性策略附近，并达到阶为$1/K$的价值近似误差；此外，我们在非退化扩散噪声下证明了一个匹配的下界。这种增加的表达能力伴随着统计成本。当漂移由神经网络参数化时，增加$K$改善近似但增加了统计复杂性。平衡这两项得到通用神经网络漂移的阶为$\tilde{O}(n^{-2/(m+6)})$的有限样本性能差距，以及单侧耗散漂移类的更优速率$\tilde{O}(n^{-2/(m+4)})$，其中$n$是样本量，$m$是状态空间的维度。数值实验为$K$的样本依赖权衡提供了经验证据，支持两种理论框架。我们的框架也提出了一个实践实现原则：根据可用样本量选择扩散预算$K$，然后选择具有相应固定Lipschitz系数的神经网络架构。

## Abstract
Diffusion-based policies have recently emerged as powerful policy parameterizations for reinforcement learning, representing state-conditioned action distributions as terminal laws of diffusion processes with parameterized drifts. This terminal-law representation has shown substantial expressive flexibility in practice, enabling diffusion policies to model complex, multimodal, and highly non-Gaussian action distributions; however, it remains unclear what mathematically drives this expressivity and how to fully exploit it when the policy is learned from finite data. In this paper, we identify the drift Lipschitz budget $K$ as a central quantity governing the expressivity and statistical behavior of diffusion policies. We quantify expressivity through approximation: diffusion policies with $K$-Lipschitz drifts can concentrate near optimal deterministic policies and achieve value approximation error of order $1/K$; moreover, we prove a matching lower bound under nondegenerate diffusion noise. This increased expressivity comes with a statistical cost. When the drift is parameterized by neural networks, increasing $K$ improves approximation but increases statistical complexity. Balancing these two terms yields a finite-sample performance gap of order $\tilde{O}(n^{-2/(m+6)})$ for generic neural-network drifts, and a sharper rate $\tilde{O}(n^{-2/(m+4)})$ for one-sided dissipative drift classes, where $n$ is the sample size and $m$ is the dimension of the state space. Numerical experiments provide empirical evidence for the sample-dependent trade-off in $K$, supporting both theoretical regimes. Our framework also suggests a practical implementation principle: choose the diffusion budget $K$ according to the available sample size, and then select a neural-network architecture with the corresponding fixed Lipschitz coefficient.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：扩散策略（diffusion-based policies）在强化学习（RL）中展现出强大的表达能力，能建模复杂、多模态、非高斯的动作分布，并在多阶段操作、运动控制等任务中取得成功。然而，其表达能力的数学根源尚未明确，且在有限样本下如何充分利用这种表达能力以实现最优性能（近似-估计权衡）缺乏理论指导。
- **核心问题**：
  1. 扩散策略的表达能力由什么核心量控制？如何影响对最优策略的近似？
  2. 增加表达能力对有限样本下的统计误差有何影响？是否存在一个最优的权衡点？
- **整体含义**：本文首次将扩散策略的漂移Lipschitz常数 \(K\) 确立为控制表达性与统计行为的关键量，并给出了理论上的上限和下限，以及神经网络参数化下的有限样本误差阶，为实践中选择扩散模型大小提供了理论依据。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：扩散策略通过随机微分方程（SDE）将简单的参考分布（如高斯）演化为复杂的终端动作分布，其表达能力主要由漂移函数的Lipschitz预算 \(K\) 决定。本文分析并量化了 \(K\) 对近似误差和统计复杂度的双重影响。
- **关键理论与技术细节**：
  1. **表达能力上界**：构造了一个线性均值回归漂移（式 3.1），证明了扩散策略可以在终端时刻以 \(O(1/K)\) 的均方误差逼近任意确定性最优动作（引理 3.1）。进而建立值逼近的上界（定理 3.2）。
  2. **表达能力下界**：在非退化扩散噪声下（状态无关或状态相关但满足均匀椭圆性），证明了对有限最优动作集，任何 \(K\)-Lipschitz 漂移的策略都不能以快于 \(O(1/K)\) 的速率集中（命题 3.3），该下界是匹配的。
  3. **有限样本学习误差**：假设漂移由 ReLU 神经网络参数化（\(K\)-Lipschitz），将学习误差分解为扩散近似误差（\(1/K\)）、神经网络实现误差（\(s^{-2/m}\)）和统计估计误差（\(\sqrt{psK/n}\) 或 \(\sqrt{ps/n}\)）。通过平衡这三项，得到泛化误差阶：通用神经网络为 \(\tilde{O}(n^{-2/(m+6)})\)；若漂移满足单边耗散性，则统计项中的 \(K\) 因子消失，速率提升至 \(\tilde{O}(n^{-2/(m+4)})\)（定理 4.2 及 Remark 4.2）。
  4. **策略梯度公式**：针对扩散策略无法直接获得终端密度的问题，利用 Girsanov 定理推导出路径似然比形式的策略梯度（命题 5.2），用于训练。
- **算法流程**：选择 ReLU 网络类 \(F_{K,s}\)，通过网络结构约束确保 Lipschitz 性质；基于共享随机变量构建经验值函数；通过最大化经验值函数得到学习策略；根据理论指导选择样本依赖的 \(K\)。

### 实验设计

- **实验设置**：
  - **实验 1（通用漂移）**：一个二维非线性多步 MDP，奖励定义为负阶段成本，包含参考最优策略 \(g_{\text{ref}}\)。动作空间标量有界，状态转移含非线性项和噪声。使用通用两层 ReLU 网络作为漂移，训练基于策略梯度的 actor-critic。
  - **实验 2（耗散漂移）**：一步上下文赌博机（contextual bandit），状态 \(X \sim \text{Unif}([0,1]^m)\)，动作空间 \(\mathbb{R}^d\)，奖励为 \(-\|a - g^*(x)\|^2\)。中心函数 \(h_W(x)\) 通过随机特征 + 岭回归拟合，且固定中心网络大小 \(s(n)\) 后，改变 \(K\)，漂移为均值回归形式（耗散性）。
- **Benchmark**：没有与其他基线方法对比，主要考察同一类策略在不同 \(K\) 下的性能，验证理论预测的样本依赖最优 \(K\) 和两种统计速率。
- **对比方法**：仅在自身设置下比较不同 \(K\) 和不同样本量 \(n\) 的影响，未涉及其他策略类（如高斯、混合高斯等）。

### 资源与算力

- 论文未明确说明使用的 GPU 型号、数量或训练时长。仅在实验设置中提及使用 `Adam` 优化器，每轮训练50次策略梯度更新，评估时取50个独立种子。计算资源细节缺失。

### 实验数量与充分性

- **实验数量**：
  - 实验1：考察了 \(K \in \{2,4,8,12,16,24,32,48,64,96,128,256,512\}\) 共13个值，样本量 \(n \in \{64,256,2048\}\)，每个设置50个独立种子，评估用1024个测试回合。结果以图1展示。
  - 实验2：考察了 \(K\) 更多值（序列长度为17），\(n \in \{128,256,512,1024,2048\}\)，每个设置5个独立种子，评估用1024个上下文。结果以图2展示。
- **充分性与客观性**：实验设计较好地验证了理论预测的两种不同权衡模式（U型 vs 单调递减至平台）。实验覆盖了不同样本量、不同 $K$，并提供了置信区间和标准误差。但缺少与其他策略类的比较，且未在更高维或真实机器人任务上验证。整体上，作为理论工作的实验支撑是充分且公平的。

### 论文的主要结论与发现

1. **漂移 Lipschitz 预算 \(K\) 是核心控制量**：扩散策略的近似误差为 \(O(1/K)\)，且该速率在非退化噪声下是最优的（匹配下界）。
2. **存在近似-估计权衡**：增加 \(K\) 提高近似能力，但增大了神经网络漂移类的复杂度，导致统计误差增大。最优 \(K\) 应随样本量 \(n\) 增长。
3. **有限样本泛化速率**：对于通用 ReLU 漂移类，学习误差阶为 \(\tilde{O}(n^{-2/(m+6)})\)；对于单边耗散类，可提升至 \(\tilde{O}(n^{-2/(m+4)})\)。
4. **实践指导原则**：根据可用样本量选择 \(K\)，然后构造相应 Lipschitz 常数的神经网络漂移。
5. **数值实验验证**：实验1显示 U 型差异，实验2显示单调递减，分别对应通用和耗散两种理论场景，支持了样本依赖的 \(K\) 选择。

### 优点

- **理论贡献突出**：首次建立了扩散策略表达性与统计代价之间的定量权衡，给出了匹配的上下界，属于该领域的理论奠基工作。
- **分析框架清晰**：将误差分解为扩散近似、网络实现和统计估计三项，便于理解。
- **策略梯度公式的推导**：为连续时间扩散策略提供了可训练的理论依据。
- **数值实验针对性强**：巧妙地设计了两个实验分别验证两种理论率，且结果与预测吻合。

### 不足与局限

- **实验覆盖有限**：仅在简单合成 MDP 和 context bandit 上验证，未使用标准 RL 基准（如 MuJoCo、DM Control）或机器人任务；未与其他策略类（如高斯、混合高斯）进行性能对比。
- **计算资源未报告**：缺少训练时间、GPU 型号等细节，可复现性受影响。
- **假设较强**：如状态空间为 \([0,1]^m\)，最优确定性策略有界且 Lipschitz，Bellman 间隙为二次上界等，在实际问题中难以严格满足。
- **忽略优化误差**：理论分析仅考虑统计误差和近似误差，实际训练中的优化困难（如策略梯度的方差）可能使结果偏离理论。
- **未讨论泛化到连续状态空间更大维度时的曲线**：随着 \(m\) 增大，理论速率变差，但实验仅用了 \(m=2\) 和 \(m=4\)。

（完）

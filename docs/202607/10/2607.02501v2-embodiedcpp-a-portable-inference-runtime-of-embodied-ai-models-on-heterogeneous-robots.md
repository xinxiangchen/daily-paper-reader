---
title: "Embodied.cpp: A Portable Inference Runtime of Embodied AI Models on Heterogeneous Robots"
title_zh: Embodied.cpp：面向异构机器人的具身AI模型可移植推理运行时
authors: "Ling Xu, Chuyu Han, Borui Li, Hao Wu, Shiqi Jiang, Ting Cao, Chuanyou Li, Sheng Zhong, Shuai Wang"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02501v2"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 用于包括VLA在内的具身AI模型的可移植推理运行时
tldr: "现有具身AI模型部署依赖碎片化的Python栈和特定后端，缺乏针对闭环控制中多速率执行和延迟优先推理的运行时。本文提出Embodied.cpp，一个便携C++推理运行时，通过五层抽象（输入适配器、序列构建器、骨干执行、头插件、部署适配器）实现模块化多速率执行与延迟优先融合推理。在HY-VLA和pi0.5模型上分别达到100%和91%任务成功率，并将WAM块内存从312.2 MiB降至88.1 MiB。该工作显著提升异构设备上的部署效率，同时保持高精度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02501v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1784, \"height\": 892, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02501v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1779, \"height\": 965, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02501v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1786, \"height\": 1048, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02501v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1782, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02501v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1790, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02501v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1789, \"height\": 215, \"label\": \"Table\"}]"
motivation: 现有推理运行时面向请求-响应服务，不满足具身部署的闭环控制中多速率执行、延迟优先推理和可扩展接口需求。
method: 通过分析VLA和WAM模型共同执行路径，设计五层抽象运行时，支持模块化多速率执行、延迟优先融合推理及可扩展算子和I/O。
result: "HY-VLA和pi0.5任务成功率100%和91%；WAM块内存从312.2 MiB降至88.1 MiB。"
conclusion: Embodied.cpp在异构机器人上实现高效部署，同时保持高模型准确率，为具身AI提供统一推理运行时。
---

## 摘要
具身AI模型现在涵盖视觉-语言-动作（VLA）模型与世界动作模型（WAM），但实际部署仍分散在特定模型的Python栈、后端假设以及机器人侧胶水代码中，尤其在异构边缘设备上。现有推理运行时主要设计用于请求-响应服务，因此不满足具身部署的运行时契约：闭环控制内的多速率执行、异构硬件上延迟优先的批次为1的推理，以及超越固定令牌I/O的可扩展具身接口。我们提出Embodied.cpp，一个面向具身模型的可移植C++推理运行时。基于代表性VLA模型和WAM的架构分析，Embodied.cpp捕获共享执行路径并将其组织为五层：输入适配器、序列构建器、骨干执行、头部插件和部署适配器。该运行时提供模块化多速率执行、延迟优先的融合推理以及可扩展的算子与I/O支持，通过一个后端抽象实现跨异构设备、机器人和模拟器的部署。我们在两个VLA模型（HY-VLA和pi0.5）以及一个使用LingBot-VA Transformer块的初步WAM基准上评估Embodied.cpp。VLA部署实现了成功的闭环执行，任务成功率分别为100.0%和91.0%。WAM基准将块内存从312.2 MiB减少至88.1 MiB。这些结果表明，Embodied.cpp在保持不同具身模型架构高精度的同时，提高了部署效率。

## Abstract
Embodied AI models now span vision-language-action (VLA) models and world-action models (WAMs), but practical deployment remains fragmented across model-specific Python stacks, backend assumptions, and robot-side glue code, especially on heterogeneous edge devices. Existing inference runtimes are designed mainly for request-response serving and therefore do not satisfy the runtime contract of embodied deployment: multi-rate execution inside closed-loop control, latency-first batch-1 inference on heterogeneous hardware, and extensible embodied interfaces beyond fixed token I/O. We present Embodied$.$cpp, a portable C++ inference runtime for embodied models. Based on an architectural analysis of representative VLA models and WAMs, Embodied$.$cpp captures a shared execution path and organizes it into five layers: input adapters, sequence builders, backbone execution, head plugins, and deployment adapters. The runtime provides modular multi-rate execution, latency-first fused inference, and extensible operator and I/O support, enabling deployment across heterogeneous devices, robots, and simulators through one backend abstraction. We evaluate Embodied$.$cpp on two VLA models, HY-VLA and pi0.5, and on a preliminary WAM benchmark using a LingBot-VA Transformer block. The VLA deployments achieve successful closed-loop execution with 100.0% and 91.0% task success rates, respectively. The WAM benchmark reduces block memory from 312.2 MiB to 88.1 MiB. These results show that Embodied$.$cpp improves deployment efficiency while preserving high accuracy across diverse embodied model architectures.

---

## 论文详细总结（自动生成）

# Embodied.cpp：面向异构机器人的具身AI模型可移植推理运行时

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有具身AI模型（VLA和WAM）的部署依赖碎片化的Python栈、特定后端假设和机器人侧胶水代码，尤其在异构边缘设备（如Jetson、RK系列、x86边缘盒子）上难以统一。
- **背景现状**：通用推理运行时（如llama.cpp、ONNX Runtime、SGLang、vLLM-Omni）主要面向请求-响应服务，不满足具身部署的特殊需求：
  - **多速率执行**：闭环控制中感知、骨干、预测头等组件需不同刷新频率。
  - **延迟优先的batch-1推理**：优化目标为低延迟、低抖动，而非吞吐量。
  - **可扩展具身接口**：输入传感器多样（图像、语言、力觉等），输出包括动作令牌、连续向量、未来预测等。
- **整体意义**：缺乏统一、可移植的推理运行时是具身AI从模型研究走向实际机器人的关键瓶颈，本文的Embodied.cpp旨在填补这一空白。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：通过分析VLA和WAM模型的共享执行路径，设计一个五层抽象的可移植C++推理运行时，将共同路径作为基础设施，差异部分作为插件。
- **五层架构**：
  1. **输入适配器（Input Adapter）**：统一处理在线传感器流（相机、力传感器、IMU）和离线数据集（LIBERO、DROID等），将多模态数据标准化为运行时内部表示。
  2. **序列构建器（Sequence Builder）**：构建模型所需的输入序列（如图像令牌、文本令牌、历史状态），支持不同模态的对齐与拼接。
  3. **骨干执行（Backbone Execution）**：执行Transformer等通用骨干网络，可利用不同的硬件后端（CPU、GPU、NPU）。
  4. **头部插件（Head Plugin）**：支持可替换的模型特定输出头（如动作解码器、未来预测头），使新模型可插入而无需修改核心运行时。
  5. **部署适配器（Deployment Adapter）**：桥接运行时输出到模拟器（如Isaac Sim）或真实机器人控制栈。
- **关键技术细节**：
  - **模块化多速率执行**：明确暴露可配置的刷新策略和共享状态池，使不同组件可独立设置运行频率（如感知层低频、控制层高频）。
  - **延迟优先融合执行**：针对batch-1场景优化，包括图重放、缓冲区复用、算子融合、后端特定调度及宿-设备数据移动管理。
  - **可扩展算子与I/O支持**：提供类型化的具身接口和“具身AI内核仓库”，收集可复用算子（如专用视觉预处理、动作头kernel），支持自定义算子注册。
- **无公式/算法伪代码**：论文以架构分析和系统设计为主，未给出具体数学公式或伪代码。

## 3. 实验设计：数据集、场景、基准与对比方法

- **VLA模型评估**：
  - **模型**：HY-VLA（基于Hunyuan-VL骨干）和pi0.5（基于PaliGemma骨干）。
  - **任务/场景**：
    - HY-VLA：RoboTwin的`place_empty_cup`任务，使用对应检查点。
    - pi0.5：使用其C++部署配置，未指定具体任务名称（推测为通用操纵任务）。
  - **基准**：报告成功率、动作块长度、服务器端推理延迟、环境步延迟、峰值GPU内存。无直接对比其他运行时，而是展示Embodied.cpp可以成功闭环执行。
- **WAM评估（初步）**：
  - **模型**：LingBot-VA的第一个Transformer块。
  - **对比**：Python原始实现（BF16权重） vs Embodied.cpp的C++实现（GGUF Q4_K量化）。
  - **指标**：单块延迟、权重内存、平均绝对误差（MAE）和余弦相似度。
  - **数据集**：100个随机输入样本。
- **未包含的对比**：论文在表格2中定性对比了llama.cpp、ONNX Runtime、SGLang、vLLM-Omni、vla.cpp，但实验部分未直接与这些运行时进行定量对比（仅在覆盖度和特性上比较）。

## 4. 资源与算力

- **文中说明**：
  - VLA部署测试中给出了峰值GPU内存（HY-VLA 6850 MiB，pi0.5 6546 MiB）。
  - WAM微基准中提到了“constrained local edge setup”（受限的本地边缘设置），但未明确GPU型号、CPU型号、训练/推理时长。
  - 未提及训练过程，只涉及推理部署。
- **结论**：论文未明确说明所用计算资源的详细规格（如GPU型号、数量、训练时长等），资源信息不完整。

## 5. 实验数量与充分性

- **实验组数**：
  - 完整VLA闭环执行：2个模型各1组（HY-VLA、pi0.5）。
  - WAM单块量化微基准：1组（LingBot-VA单块）。
  - 无消融实验、无不同硬件对比、无多任务变体。
- **充分性评价**：
  - **实验覆盖**：仅验证了两个VLA模型和一个WAM块，未测试完整WAM闭环（作者指出全模型在本地边缘设备上不稳定）；未在不同边缘设备（如Jetson、RK3588）上测试；未与其他运行时（如vla.cpp）做同条件性能对比。
  - **客观性**：成功率、延迟、内存指标清晰；量化精度损失极小（MAE < 3.3e-2，余弦相似度 > 0.9997），结果合理。
  - **公平性**：VLA对比的是Python原始实现？实际上VLA实验只报告了Embodied.cpp本身的结果，未与基线对比。WAM对比了Python原始BF16 vs Embodied.cpp Q4_K量化，但未控制后端（Python使用PyTorch，C++使用GGUF），量化本身会引入额外偏差。
- **总体**：实验设计初步，主要用于Proof-of-Concept，充分性有限。

## 6. 主要结论与发现

- **VLA部署结论**：Embodied.cpp能成功支持不同VLA架构的闭环执行。
  - HY-VLA：成功率100.0%，步延迟735.9 ms，推理延迟1340.3 ms，VRAM 6850 MiB。
  - pi0.5：成功率91.0%，步延迟56.85 ms，推理延迟266.6 ms，VRAM 6546 MiB。
- **WAM微基准结论**：Embodied.cpp的C++量化实现可在极小精度损失下显著降低内存。
  - 单块内存：312.2 MiB（BF16） → 88.1 MiB（Q4_K），减少约71.8%。
  - 延迟基本持平（3.236 ms vs 3.171 ms）。
- **总体**：Embodied.cpp在保持高模型精度的同时，提升了部署效率（内存、延迟），验证了统一运行时在异构机器人上的可行性。

## 7. 优点：方法或实验设计上的亮点

- **方法设计亮点**：
  - 系统性地分析VLA和WAM家族，提炼出共享执行路径，避免为每个新模型重复构建运行时。
  - 五层抽象清晰分离了输入处理、序列构建、骨干计算、任务特定头以及外部适配，具有良好可扩展性。
  - 专门针对具身部署的关键挑战（多速率、延迟优先、异构硬件）设计，不简单复用LLM运行时的设计。
  - 开源C++实现，便于在资源受限的边缘设备上运行。
- **实验设计亮点**：
  - 在真实机器人任务（RoboTwin）上测试VLA模型，提供了闭环成功率，而非仅跑通推理。
  - WAM部分虽然只是单块微基准，但量化前后精度对比充分（MAE + Cosine），证明了C++实现与原始实现的数值一致性。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅测试两个VLA模型，未覆盖AR-Token VLA（如OpenVLA）、Hierarchical/Asynchronous VLA等更多子类型。
  - WAM仅测试单块，未进行完整模型闭环控制实验（作者承认全模型不稳定）。
  - 未在不同机器人平台（如Jetson Orin、RK3588、x86工控机）上重复实验验证可移植性声明。
  - 未与其他具身推理运行时（vla.cpp）或通用运行时（ONNX Runtime）在相同模型、硬件条件下定量对比。
- **偏差风险**：
  - VLA的成功率可能只针对单一任务，泛化性未知；HY-VLA的100%成功率样本量未报告（置信区间给出了[83.9,100.0]）。
  - WAM量化对比中，Python版本使用了BF16，C++版本使用了Q4_K，差异不仅来自后端实现，还来自量化精度，无法单独衡量运行时的效率。
- **应用限制**：
  - 五层架构虽清晰，但新增模型可能需要实现新的头部插件或输入适配器，仍有一定开发成本。
  - 多速率执行的具体调度策略未详细设计或评估（如如何在运行时动态调整速率）。
  - 论文未讨论调试、安全性或实时性保证（如任务关键系统中的硬实时要求）。

（完）

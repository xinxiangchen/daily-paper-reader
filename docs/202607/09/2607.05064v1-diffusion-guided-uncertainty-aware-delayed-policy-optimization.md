---
title: Diffusion-Guided Uncertainty-Aware Delayed Policy Optimization
title_zh: 扩散引导的不确定性感知延迟策略优化
authors: "Junqi Tu, Zejiao Liu, Fangfei Li, Yang Tang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05064v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 基于扩散模型的延迟反馈强化学习策略优化
tldr: 现实环境中强化学习常因延迟反馈而性能严重下降。现有方法通过构建增强状态或预测真实状态缓解观测延迟，但忽略了随机MDP下延迟状态与真实状态的内在差异。本文理论证明该差异导致最优策略退化，并提出扩散引导的不确定性感知延迟策略优化（DUPO），利用扩散模型显式建模延迟消息与当前状态的关系，基于差异估计加权延迟策略。在多种随机延迟的连续机器人控制任务中，DUPO一致优于现有方法，在长延迟和随机延迟场景下仍保持有效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05064v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 796, \"height\": 445, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05064v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1806, \"height\": 725, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05064v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1547, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05064v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 865, \"height\": 328, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05064v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1664, \"height\": 2222, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05064v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1510, \"height\": 622, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05064v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 773, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05064v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 765, \"label\": \"Table\"}]"
motivation: 现有延迟反馈强化学习方法忽略了延迟状态与真实状态在随机MDP下的内在差异，导致最优策略退化。
method: 提出DUPO，使用扩散模型建模延迟状态与当前状态的关系，并利用差异估计对延迟策略进行加权。
result: 在多种随机延迟的连续机器人控制任务中，DUPO一致优于现有方法，在长延迟和随机延迟场景下仍有效。
conclusion: DUPO通过显式建模延迟-真实状态差异有效提升了延迟反馈下的强化学习性能。
---

## 摘要
在实际环境中的强化学习常常因反馈延迟而导致性能严重下降。现有方法通常通过构建增强状态或预测真实状态来缓解由观测延迟引起的性能退化。然而，这些方法往往忽略了随机MDP导致的延迟状态与真实状态之间的固有差异。我们从理论上证明了这种差异的存在，并表明它会导致最优策略的退化。为解决这一挑战，我们提出了扩散引导的不确定性感知延迟策略优化（DUPO）。我们的方法使用扩散模型显式建模延迟状态信息与当前状态之间的关系，并利用由此产生的差异估计来加权延迟策略。在多个随机延迟的连续机器人控制任务上的大量实验表明，即使是在长延迟和随机延迟场景下，DUPO也持续优于现有方法，并保持有效性。

## Abstract
Reinforcement learning in real world environments often suffers from severe performance degradation due to delayed feedback. Existing approaches typically mitigate performance degradation caused by observation delays by constructing augmented states or predicting the true states. However, these methods often overlook the inherent discrepancy between delayed state and true states induced by stochastic MDP. We theoretically prove the existence of such a discrepancy and show that it leads to the degradation of the optimal policy. To address this challenge, we propose Diffusion Guided Uncertainty Aware Delayed Policy Optimization (DUPO). Our method explicitly models the relationship between delayed state message and the current state using a diffusion model, and leverages the resulting discrepancy estimates to weight delayed policies. Extensive experiments on continuous robotic control tasks with multiple stochastic delays demonstrate that DUPO consistently outperforms existing methods and remains effective even under long and random delay scenarios.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现实环境中的强化学习（RL）常因反馈延迟（observation delay）而遭受严重性能下降，甚至导致安全关键性失败。现有方法（如记忆无关法、状态增强法、模型预测法）虽然在一定程度上缓解了延迟问题，但普遍忽略了在随机MDP（stochastic MDP）中，延迟状态与真实状态之间存在内在的、随延迟长度而放大的差异——即“延迟引起的状态差异”（delay-induced state discrepancy）。
- **背景**：随机MDP中，环境内在地具有噪声，导致即使使用延迟状态和动作历史也无法完全恢复当前真实状态，导致任何基于单点状态估计的策略都会面临不可避免的性能损失。论文理论证明：延迟越长，这种差异越大，并导致最优策略退化。因此，需要显式建模延迟后验分布，并利用不确定性来指导策略更新。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 提出 **扩散引导的不确定性感知延迟策略优化（DUPO）**，核心思路是：
  - 使用条件扩散模型（conditional diffusion model）来学习延迟消息 \( M_t = (s_{t-\Delta T}, a_{t-\Delta T:t-1}) \) 与当前真实状态 \( s_t \) 之间的条件分布 \( p_\theta(s_t | M_t) \)。
  - 从该分布中采样多个可能的真实状态，估计行动价值函数（Q函数）在延迟下的不确定性（方差），并用该不确定性自适应地加权策略更新，从而得到延迟自适应的策略。

### 关键技术细节
1. **延迟消息构造**：将延迟观测 \( s_{t-\Delta T} \) 与中间动作历史拼接为 \( M_t \)。
2. **扩散模型建模条件分布**：
   - 前向扩散：按固定方差计划添加高斯噪声。
   - 反向去噪：以 \( M_t \) 为条件，训练噪声预测网络 \( \epsilon_\theta(s_t^n, M_t, n) \)。
   - 损失函数：标准噪声预测目标 \( \mathbb{E}[\|\epsilon - \epsilon_\theta(s_t^n, M_t, n)\|^2] \)。
3. **不确定性估计**：从训练好的扩散模型中采样 \( N \) 个可能的当前状态 \( s_t^{(i)} \sim p_\theta(\cdot | M_t) \)，计算每个动作 \( a \) 对应的 Q 值方差：
   \[
   \widehat{\operatorname{Var}}[Q(M_t, a)] = \frac{1}{N}\sum_i Q_\psi(s_t^{(i)}, a)^2 - \overline{Q_\psi(s_t, a)}^2
   \]
4. **不确定性加权策略**：对基础策略 \( \pi_\phi(a|M_t) \) 进行逆方差加权：
   \[
   \omega(M_t, a) = \frac{\beta}{\widehat{\operatorname{Var}}[Q(M_t, a)] + \varepsilon}
   \quad\Rightarrow\quad
   \pi_\phi'(a|M_t) \propto \omega(M_t, a)\,\pi_\phi(a|M_t)
   \]
   将加权策略整合进SAC框架，使用延迟-真实过渡元组训练critic和actor。

## 3. 实验设计

### 使用场景与数据集
- **环境**：MuJoCo-v4 连续控制基准（Ant, HalfCheetah, Hopper, Walker2d, Swimmer）。
- **延迟设置**：将原确定性环境转换为随机MDP（动作执行添加高斯噪声 \( \mathcal{N}(0,0.1) \)），再施加随机观测延迟 \( \Delta T \sim \text{Uniform}\{1, \dots, \Delta T_{\max}\} \)，分别设置 \( \Delta T_{\max} \) 为 5、10、25。

### Benchmark 与对比方法
- **对比基线**：
  - **DC/AC** (Bouteiller et al. 2020)：SAC变体，利用off-policy多步价值估计和部分轨迹重采样。
  - **State Augmentation** (Wang et al. 2024)：将延迟观测与动作历史拼接构建时间校准输入。
  - **State Prediction** (Wang et al. 2024)：训练辅助模型预测延迟-真实状态，基于预测状态进行控制。
- **评估协议**：所有方法使用相同环境构造、交互预算（1M全局步数）和评估协议，报告最终回报的均值和标准差（4个随机种子的归一化结果）。

## 4. 资源与算力

- **论文未明确说明**使用的 GPU 型号、数量、训练时长等具体算力资源。
- 仅提供了超参数表（batch_size=32, max_epoch=200, step_per_epoch=5000 等），但未提及硬件配置或训练时间。

## 5. 实验数量与充分性

- **主要实验**：
  - 在 5 个 MuJoCo 环境下，对 3 种延迟设定（\( \Delta T_{\max}=5,10,25 \)）进行测试，共 15 个主要比较实验。
  - 每个实验使用 4 个随机种子，报告平均回报和标准差。
- **消融实验**：
  - 延迟分布消融（均匀、截断高斯、截断泊松），在 HalfCheetah 上 \( \Delta T_{\max}=10 \)。
  - 后验采样机制消融（扩散模型 vs. Monte Carlo Dropout vs. 单峰高斯采样）。
  - 超参数敏感性：对不确定性权重系数 β 和采样数 N 进行调节。
- **充分性评价**：
  - 实验覆盖了多个环境、多种延迟长度、多个基线，且包含消融和敏感性分析，整体较为充分和客观。
  - 但未在真实世界的物理系统上进行验证，所有实验均为仿真环境，可能存在 sim-to-real 偏差。

## 6. 主要结论与发现

1. **理论结论**：随机MDP中延迟引起的状态差异会随延迟长度线性或超线性放大，任何单点状态估计方法都会导致策略退化（性能损失下界与延迟长度成正比）。
2. **方法有效性**：DUPO 在所有环境和延迟设置中普遍优于 DC/AC、State Prediction 和 State Augmentation，尤其在长延迟（\( \Delta T_{\max}=25 \)）下优势明显，回报提升数倍。
3. **鲁棒性**：DUPO 对不同延迟分布（均匀、高斯、泊松）表现稳定，扩散模型的多模态后验采样优于 MC dropout 或单峰高斯采样。
4. **收敛性与稳定性**：学习曲线显示 DUPO 更快达到高回报区域，且训练方差更小、后期平台期更短。

## 7. 优点

- **理论创新**：首次严格证明延迟在随机MDP中引起不可约状态差异，并推导出单点估计策略的性能损失下界，为后续方法提供理论依据。
- **方法设计**：有机结合扩散模型的生成能力（捕捉多模态后验）与不确定性加权策略优化，实现延迟自适应行为，避免了状态空间膨胀（如状态增强法）和单点预测的信息损失。
- **实验全面**：在多种延迟设置、环境、分布下进行对比，消融实验系统，结论可靠。
- **性能突出**：在多数任务上以较大优势领先强基线，尤其长延迟场景下表现稳定。

## 8. 不足与局限

- **算力资源未披露**：无法评估方法计算成本或可伸缩性。
- **仅仿真环境**：未在真实机器人系统上验证，延迟噪声和MDP随机性模型与实际可能有差距。
- **假设限制**：假设延迟上界已知且分布固定；实际中延迟可能非平稳或未知分布，需进一步研究。
- **SAC基础**：方法基于Soft Actor-Critic，可能不适用于离散动作或高维图像输入场景。
- **超参数敏感**：β 和 N 对性能有影响（虽然相对稳定），需要调参。
- **消融实验环境有限**：部分消融仅在 HalfCheetah 或 Hopper 上进行，未在所有环境下验证。

（完）

---
title: "Mixture of Frames Policy: Multi-Frame Action Denoising for Bimanual Mobile Manipulation"
title_zh: 帧混合策略：双手机器人操作的多帧动作去噪
authors: "Dian Wang, Jisang Park, Xiaomeng Xu, Han Zhang, Shuran Song, Jeannette Bohg"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11884v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 多帧动作去噪扩散策略用于双臂移动操作
tldr: 机器人操作任务需在不同坐标系下执行，但现有扩散策略固定单一动作帧，导致去噪器建模复杂分布。本文提出Mixture of Frames Policy (MoF)，在多坐标系同步去噪，通过可微变换融合帧专用去噪器预测。在9个仿真和2个真实双机械臂移动操作任务中，MoF优于最优单帧和MoE基线，证明帧选择具有任务依赖性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11884v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1448, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 577, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1450, \"height\": 672, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1446, \"height\": 668, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 741, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1438, \"height\": 1108, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11884v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 926, \"height\": 615, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11884v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 252, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11884v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1449, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11884v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 390, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11884v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1370, \"height\": 426, \"label\": \"Table\"}]"
motivation: 现有扩散策略固定单一动作帧，迫使去噪器建模复杂分布，限制多帧协调任务性能。
method: MoF维护单一规范扩散状态，变换到多个任务相关帧，应用帧专用去噪器，再融合回规范帧实现同步去噪。
result: 9个仿真和2个真实双机械臂移动操作任务中，MoF超越最优单帧和MoE基线，验证多帧去噪优势。
conclusion: MoF通过多帧同步去噪显著提升双机械臂移动操作性能，表明动作帧选择因任务而异。
---

## 摘要
机器人操作本质上是多帧的：局部动作在末端执行器坐标系中可能很简单，而运输、直立物体处理以及全身协调则在基座对齐坐标系中更好地表示。然而，当前基于扩散的视觉运动策略通常采用单一的预定义动作帧，迫使一个去噪器去建模在该帧中往往不必要地复杂的动作分布。我们提出了帧混合策略（MoF），一种跨多个坐标帧执行同步动作去噪的扩散策略。MoF维护一个单一的规范扩散状态，将其在多个任务相关帧中重新表示，应用帧专用的去噪器，然后将它们的噪声预测融合回规范帧。为了实现这一过程在中间噪声扩散状态下的可行性，我们引入了一种基于列的6D旋转表示，该表示位于SE(3)动作参数化内，支持精确、可微的帧变换，而无需将噪声旋转限制在SO(3)流形上。在九个模拟的双手机器人操作任务中，我们展示了最佳动作帧是任务相关的，并且MoF相较于oracle帧选择和标准混合专家（MoE）基线有所改进。我们进一步在两个真实世界的双手机器人移动操作任务上评估了MoF，结果表明它优于所有组成的单帧基线。项目主页：https://mofpo.github.io

## Abstract
Robotic manipulation is inherently multi-frame: local actions may be simple in an end-effector frame, while transport, upright-object handling, and whole-body coordination are better represented in a base-aligned frame. However, modern diffusion-based visuomotor policies typically commit to a single predefined action frame, forcing one denoiser to model action distributions that are often unnecessarily complex in that frame. We propose Mixture of Frames Policy (MoF), a diffusion policy that performs synchronized action denoising across multiple coordinate frames. MoF maintains a single canonical diffusion state, re-expresses it in several task-relevant frames, applies frame-specialized denoisers, and fuses their noise predictions back in the canonical frame. To make this possible for intermediate noisy diffusion states, we introduce a column-based 6D rotation representation within an SE(3) action parameterization that supports exact, differentiable frame transformations without requiring noisy rotations to lie on the SO(3) manifold. Across nine simulated bimanual manipulation tasks, we show that the best action frame is task-dependent and that MoF improves over oracle frame selection and standard Mixture-of-Experts (MoE) baselines. We further evaluate MoF on two real-world bimanual mobile manipulation tasks, demonstrating that it outperforms all constituent single-frame baselines. Project homepage: https://mofpo.github.io

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现代基于扩散的视觉运动策略（如Diffusion Policy）通常采用单一预定义的动作坐标帧（如世界帧、末端执行器帧等），迫使一个去噪器去建模在该帧中可能过于复杂的动作分布。然而，双手机器人操作任务天然涉及多个坐标系（如基座帧、左右末端执行器帧、相对轨迹帧等），且不同子任务（如抓取、运输、协调操作）的最佳表示帧不同。单一帧策略无法适应任务阶段变化，导致学习困难。
- **整体含义**：本文表明动作帧的选择对策略性能有显著且任务相关的影响，并提出一种能够跨多个坐标帧同步去噪的策略（Mixture of Frames Policy, MoF），以自动利用各帧的优势，提升双手机器人移动操作的性能和鲁棒性。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：MoF维护一个单一的规范帧扩散轨迹，在每一步去噪过程中，将当前噪声动作重新表达为多个任务相关帧，每个帧使用专门的去噪器（专家）预测噪声，然后将所有预测噪声变换回规范帧并融合（通过学习到的路由器权重或均匀权重），保持所有专家沿同一扩散轨迹同步。
- **关键技术细节**：
  - **多帧同步去噪**：设规范帧为 \(F_c\)，对于每个专家帧 \(F_m\)，先变换状态 \(x_k^c\) 至 \(F_m\)，专家预测噪声 \(\epsilon_{\theta_m}\)，再反变换回 \(F_c\)，融合后得到融合噪声 \(\hat{\epsilon}^c = \sum_{F_m \in \mathcal{F}} w_m \cdot {}^c T_m\left( \epsilon_{\theta_m}({}^m T_c(x_k^c), o; k) \right)\)。权重 \(w_m\) 可由路由器（MoF-MoE）学习或均匀分配（MoF-Ensemble）。
  - **变换兼容的动作表示**：标准扩散策略使用6D行向量旋转表示（需正交化投影到SO(3)），这会破坏噪声状态。本文提出列向量表示：将末端执行器姿态表示为位置 \(p_c\) 和旋转矩阵的前两列 \(c1_c, c2_c\)（均为3D向量）。帧变换仅需左乘帧间旋转矩阵 \({}^m R_c\)，无需正交化，因此可应用于任意噪声状态。
  - **训练损失**：混合损失 \(L = L_{\text{mix}} + \lambda_{\text{aux}} L_{\text{aux}}\)。\(L_{\text{mix}}\) 使融合后的规范帧噪声预测与真实噪声匹配；\(L_{\text{aux}}\) 是每个专家在自身帧内的辅助去噪损失，防止弱路由器导致梯度消失。
- **算法流程**（文字说明）：
  1. 采样规范帧动作 \(x_0^c\)，加噪得到 \(x_k^c\)；
  2. 对每个专家帧 \(F_m\)，将 \(x_k^c\) 变换为 \({}^m T_c(x_k^c)\)；
  3. 每个专家基于观测 \(o\) 和扩散步 \(k\) 预测噪声 \(\epsilon_{\theta_m}({}^m T_c(x_k^c), o; k)\)；
  4. 将预测噪声变换回规范帧：\({}^c T_m(\epsilon_{\theta_m})\)；
  5. 融合：\(\hat{\epsilon}^c = \sum w_m \cdot {}^c T_m(\epsilon_{\theta_m})\)；
  6. 使用标准DDIM调度器更新 \(x_k^c\)；
  7. 重复直至得到干净动作。

## 3. 实验设计

- **数据集/场景**：
  - **仿真实验**：9个双手机器人操作任务，来自BiGym（FlipCup, Move2Plates, Kitchenware, FlipSandwich, Dishwasher）和DexMimicGen（Threading, 3PieceAssembly, BoxCleanup, DrawerCleanup）。每个任务100条演示，使用RBY1机器人模型（BiGym）或桌面设置（DexMimicGen）。
  - **真实实验**：2个双手机器人移动操作任务（倒水任务、上菜任务），使用HoMMI框架，训练集分别为257和177条演示。
- **Benchmark**：仿真任务以任务成功率（%）为指标，每种方法3个随机种子，每个种子评估5个checkpoint（460-500 epoch，每10 epoch一个），每个checkpoint进行50次测试。真实任务每次20次rollout。
- **对比方法**：
  - 单帧基线：左帧、右帧、基座帧、相对轨迹帧（四种专家帧的单帧DP）。
  - Oracle Frame：每个任务选择最佳单帧（后验）。
  - Worst Frame：每个任务最差单帧。
  - DP：标准世界帧DP。
  - MoE-DP：在条件路径使用MoE，但动作仍为单帧。
  - Single-Frame Ensemble：在相同帧上集成4个去噪器。
  - MoF变体：MoF-MoE（学习路由器）、MoF-Ensemble（均匀权重）。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长**。仅在附录C.3中列出训练超参数（batch size 128，500 epochs，AdamW优化器），但未提硬件配置。真实实验部分也未提及。因此无法总结具体算力，仅知训练在普通计算集群上完成。

## 5. 实验数量与充分性

- **仿真实验**：9个任务 × 3种子 × 5 checkpoint × 50 episodes ≈ 6750次评估（仅对比方法），加上消融实验（4个代表任务，多种变体）、路由器分析（20个演示）。数量充足，覆盖不同任务类型（移动操作、桌面操作）。
- **真实实验**：2个任务，每个20次rollout，覆盖不同颜色组合和放置距离。相对规模较小但符合真实世界部署。
- **消融实验**：考察规范帧选择、去除最佳专家帧、去除辅助损失、正交化投影等，共7种变体，结果详实。
- **客观性**：使用多种随机种子，统计标准误；对比方法包括Oracle Frame（理论上界）、Worst Frame（下界）以及领域内先进基线（MoE-DP）。结果公平可靠。
- **充分性**：实验覆盖了从模拟到真实、从简单到复杂任务，验证了多帧去噪的有效性，系统性较好。

## 6. 主要结论与发现

1. **动作帧选择显著影响性能**：单帧DP下，Oracle Frame与Worst Frame平均差距15个百分点，且最佳帧因任务而异（基座帧平均最好但不绝对）。
2. **MoF优于所有单帧基线和MoE变体**：MoF-MoE平均成功率66.8%，比MoE-DP高11.7分，比DP高16.5分；在9个任务中有8个达到或超过Oracle Frame。
3. **路由器动态分配帧专家**：在Threading等任务中，路由器前期倾向于相对轨迹帧（独立到达），后期转向基座帧（协调插入），说明MoF能根据任务阶段自动选择合适帧。
4. **列向量旋转表示是关键**：若使用正交化投影，MoF-Ensemble完全失效（0%），MoF-MoE下降7个百分点，证明变换兼容表示的必要性。
5. **真实世界验证**：MoF-MoE在倒水任务（85% vs 最佳单帧70%）和上菜任务（70% vs 60%）均优于所有单帧基线，且失败模式更分散，说明多帧融合提升鲁棒性。

## 7. 优点

- **方法创新性**：首次提出在多帧空间同步去噪的扩散策略，克服了单帧限制，可自动适应不同子任务的最佳帧。
- **技术贡献**：提出列向量SE(3)动作表示，支持对噪声动作进行精确可微帧变换，不破坏扩散状态。
- **实验设计全面**：涵盖9个仿真+2个真实任务，对比多种基线，进行详尽消融和路由器分析，验证了每个组件（专家集、辅助损失、变换表示）的贡献。
- **真实世界结果稳健**：MoF在真实机器人上成功执行复杂多阶段任务（倒水、上菜），展示了实用潜力。
- **开源与复现**：提供项目主页和代码（论文末未显式声明，但通常项目主页会包含代码）。

## 8. 不足与局限

- **未扩展到大型VLA模型**：当前实验仅使用小型扩散U-Net，未验证在更大规模视觉-语言-动作模型上的效果，可扩展性待验证。
- **路由器监督信号可能不优**：路由器仅通过去噪损失训练，可能不直接对应策略执行质量，未来可探索行为感知目标。
- **依赖人工定义候选帧和精确变换**：需要设计者预定义帧集合，且依赖机器人本体感知（proprioception）提供精确帧间变换，无法处理误差或未知帧。
- **仿真与真实任务规模有限**：真实任务仅两个，且训练数据量（100-257条）较小，泛化性可能受限；仿真任务虽然9个，但均限于特定机器人平台。
- **未分析帧变换误差影响**：在实际部署中，运动学模型或传感器噪声可能导致变换不准确，论文未处理帧不确定性。
- **计算开销**：每个去噪步需运行多个专家网络，计算量约为单帧的4倍，但论文未报告推理延迟。

（完）

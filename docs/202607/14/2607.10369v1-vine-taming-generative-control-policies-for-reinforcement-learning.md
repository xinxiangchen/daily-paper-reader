---
title: "VINE: Taming Generative Control Policies for Reinforcement Learning"
title_zh: "VINE: 驯服强化学习的生成控制策略"
authors: "Rushuai Yang, Zhuo Han, Houlin Li, Hecheng Wang, Zhichao Wu, Rui Zhang, Zhaowei Zhang, Zihong Chen, Xiaohan Yan, Chiming Liu, Yi Chen, Wei Shan, Maoqing Yao"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10369v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 强化学习结合流匹配生成策略
tldr: 流匹配策略通过迭代生成动作实现复杂多模态分布建模，但在强化学习中端到端值梯度优化不稳定。现有方法避开迭代生成或牺牲表达能力。本文发现不稳定性源于采样策略，提出VINE方法，在每个去噪步重建插值状态以稳定梯度传播。VINE保留了流匹配的表达能力和迭代生成，在OGBench和真实机器人任务上超越先前方法，实现了稳定的端到端优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1414, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1300, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1284, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1465, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1500, \"height\": 653, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1213, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1544, \"height\": 1685, \"label\": \"Table\"}]"
motivation: 消除流匹配策略在值梯度强化学习中端到端优化不稳定的根本原因，保持高表达力和迭代生成。
method: 提出VINE采样方法，在每个去噪步重建新的插值状态，构建稳定可微路径以传播值梯度。
result: VINE在OGBench离线强化学习基准和真实机器人操作任务上一致优于现有最先进方法。
conclusion: 端到端值梯度优化可与迭代生成流匹配兼容，通过改进采样策略实现稳定训练和性能提升。
---

## 摘要
流匹配策略已成为机器人学习中一种有效的策略参数化方法。它们从噪声中迭代生成动作，能够高度表达复杂和多模态动作分布。然而，先前的研究观察到，将这类策略与值梯度强化学习（RL）结合扩展时，常常导致训练不稳定。现有方法将这种不稳定性归因于迭代生成，因此通过牺牲迭代生成、高表达性或值梯度优化来避免端到端的值梯度优化。与先前的观点相反，我们证明了不稳定性并非源于迭代生成本身，而是源于最初为行为克隆设计的朴素采样策略，它在值梯度RL下变得脆弱。基于这一见解，我们提出了VINE，一种面向RL的采样方法，能够为流匹配策略实现稳定的端到端值梯度优化。VINE不遵循单一流轨迹，而是在每个去噪步骤中重构一个新的插值状态，为值梯度传播创建一个稳定的可微路径，同时与原始的流匹配去噪过程保持兼容。因此，VINE在不牺牲端到端值梯度优化的情况下，保留了流匹配的表达性和迭代生成。尽管通过所有十个去噪步骤进行端到端反向传播，VINE仍然实现了稳定的策略改进，并在OGBench离线RL基准测试和真实世界机器人操作任务中持续优于最先进的RL方法。视频可在我们的网站上获取：https://agibottech.github.io/vine。

## Abstract
Flow-matching policies have emerged as an effective policy parameterization for robot learning. They iteratively generate actions from noise, enabling highly expressive modeling of complex and multimodal action distributions. However, prior works observed that scaling these policies with value-gradient reinforcement learning (RL) often leads to training instability. Existing methods attribute this instability to iterative generation and therefore avoid end-to-end value-gradient optimization by sacrificing iterative generation, high expressiveness, or value-gradient optimization. Contrary to prior belief, we show the instability does not stem from iterative generation itself, but from the vanilla sampling strategy originally designed for behavior cloning, which becomes brittle under value-gradient RL. Motivated by this insight, we propose VINE, an RL-oriented sampling method that enables stable end-to-end value-gradient optimization for flow-matching policies. Instead of following a single flow trajectory, VINE reconstructs a new interpolation state at every denoising step, creating a stable differentiable path for value-gradient propagation while remaining compatible with the original flow-matching denoising process. As a result, VINE preserves the expressiveness and iterative generation of flow-matching without sacrificing end-to-end value-gradient optimization. Despite performing end-to-end backpropagation through all ten denoising steps, VINE achieves stable policy improvement and consistently outperforms state-of-the-art RL methods on the OGBench offline RL benchmark and real-world robotic manipulation task. Videos are available on our website: https://agibottech.github.io/vine.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：流匹配（Flow-Matching）等生成式控制策略在机器人学习中被广泛用于行为克隆（BC），能够从噪声中迭代生成动作，建模复杂多模态的动作分布。
- **核心问题**：当将这些生成策略与值梯度强化学习（Value-Gradient RL）结合进行端到端优化时，现有方法普遍遭遇严重的训练不稳定性。以往工作将不稳定性归咎于迭代生成过程本身，因此采取了三种妥协策略：
  1. 固定生成策略参数，仅用外部引导信号（牺牲表达性）。
  2. 将多步生成蒸馏为单步（牺牲迭代生成）。
  3. 放弃批评者动作梯度，仅用标量值加权（牺牲值梯度优化）。
- **本文观点**：不稳定性并非来自迭代生成本身，而是来自**最初为行为克隆设计的朴素采样策略**（如纯欧拉ODE采样）。该采样策略在RL值梯度优化下变得脆弱，因为它无法支持动态的、价值驱动的模式迁移。
- **整体意义**：本文旨在**同时保留生成策略的高表达性、迭代生成和端到端值梯度优化**，提出一种面向RL的采样方法VINE，实现稳定的策略改进。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 核心思想
- **重新设计采样路径**：标准流匹配训练使用插值路径 `x_t = t*a + (1-t)*z` 训练速度场 `v_θ`，但推理时只用单条固定噪声路径（欧拉OD）。VINE在每个去噪步**重构一个新的插值状态**，注入新鲜噪声，同时用速度场预测动作估计，从而构建稳定的可微路径。

### 关键技术细节
1. **每步重构插值状态**：给定当前动作估计 `â_k`，采样新噪声 `z_k ~ N(0,I)`，构造：
   ```
   x̂_k = t_k * â_k + (1 - t_k) * z_k
   ```
   其中 `t_k` 是单调递增的时间步（`0 = t_0 < t_1 < ... < t_K = 1`），保证随步骤进行噪声权重递减。

2. **端点预测**：利用最优速度场的性质（定理1），通过：
   ```
   â_{k+1} = x̂_k + (1 - t_k) * v_θ(x̂_k, t_k; s)
   ```
   得到下一步动作估计。该公式与流匹配训练语义完全兼容：输入是插值状态，速度场保持原始形式。

3. **迭代生成**：从初始噪声 `â_0 ~ N(0,I)` 开始，递归执行上述两步，经过K步得到最终动作 `a = â_K`。

### 算法流程（文字描述）
- **训练时的行动者更新**：
  1. 输入状态 `s`，通过VINE生成动作 `â_K`（调用函数 `GENERATE(s)`）。
  2. 计算行为克隆正则项（BC系数 α），最小化损失：`-Q_ϕ(s, â_K) + α ||â_K - a||^2`。
  3. 通过**完整的K步去噪链**进行端到端反向传播（BPTT），更新速度场参数 θ。
- **批评者更新**：标准双Q学习，目标Q由目标网络给出，动作由VINE从下一状态生成。
- **在线RL**：将VINE生成的轨迹加入经验回放池（可选）。

### 理论支撑
- **定理1（FM-VINE一致性）**：最优速度场 `v*` 满足 `x + (1-t)v*(x,t;s) = E[a|x_t=x,t,s]`，无论 `x` 如何构造。因此VINE的端点预测具贝叶斯最优性。
- **推论1（确定性VINE下的收缩性）**：当动作先验为高斯时，确定性VINE的估计单调收敛到均值，且早期步骤收敛更快。

## 3. 实验设计

### 数据集与场景
- **离线RL基准**：OGBench 的8个领域共40个任务，包括：
  - 长程导航：`antmaze-large`, `antmaze-giant`, `humanoidmaze-medium`, `humanoidmaze-large`
  - 稀疏奖励操作：`scene-sparse`, `puzzle-3x3-sparse`, `cube-double`, `cube-triple`
  - 每个领域含5个不同难度的任务，总计40个。
- **真实世界在线RL任务**：插头插入插座任务（socket insertion），需要机器人从桌面拾取插头并插入固定插座，涉及接触式操作。

### 对比方法
- 按值梯度传播方式分为四类：
  - **高斯/单步策略**：ReBRAC（行为正则化高斯策略）
  - **围绕链（Around the Chain）**：FQL, FAWAC（流Q学习/加权，避免BPTT），IFQL, QAM（伴随匹配），DAC（策略约束），QSM（Q分数匹配），CGQL, CGQL-M, CGQL-L（对比能量预测），DSRL（潜空间RL），FEdit（外部编辑），FBRAC（流BRAC），BAM（伴随匹配）
  - **外部链（Outside the Chain）**：固定生成策略，外部优化动作（如FEdit, DSRL, EXPO）
  - **通过链（Through the Chain）**：SAC-Flow（直接BPTT，但认为不稳定），VINE（本方法）
- 真实世界对比：SAC-Flow, Hil-SERL, RLT, EXPO, DSRL。所有方法使用图像+本体感受输入。

### 评估指标
- 离线：每个任务12个种子，报告均值和95%置信区间（5000次自助抽样），以成功率衡量。
- 真实：20次试验的成功率、微调时间、人工干预比例。

## 4. 资源与算力

- **论文未明确说明**所使用的GPU型号、数量及训练总时长。仅提及在OGBench任务上训练1M梯度步，每20k步评估一次（共50个episodes）。真实世界任务微调时间为20分钟（VINE、Hil-SERL、SAC-Flow）。未提供具体的GPU配置信息。

## 5. 实验数量与充分性

### 实验数量
- **离线RL**：8个领域 × 5个任务 = 40个任务，每个任务12个种子，总计480次运行。
- **真实世界**：1个任务，20次试验，3类对比（SAC-Flow、Hil-SERL、VINE）+ 带BC初始化的方法（DSRL、RLT、EXPO）。
- **消融实验**：
  - 采样器消融：VINE vs 标准欧拉采样（图4，5个代表性任务）。
  - 迭代步数影响：K=1,4,6,8,10（图5，AntMaze任务）。
  - 梯度稳定性可视化（图4右侧：梯度范数曲线）。
- **理论验证**：二维玩具模拟（图2）展示概率路径差异。

### 充分性与公平性
- **充分性**：覆盖了多领域、稀疏/稠密奖励、长程/短程任务；对比了各类代表性基线（高斯、扩散、流策略，以及不同梯度传播方式）；进行了消融和理论分析。
- **公平性**：采用与QAM相同的评估协议；种子数12，置信区间报告；超参数公开（附录表3、表4）。但**未见对比方法超参数调整细节**，可能影响公平性；部分基线结果引自原文（FQL、QAM等），未复现。

## 6. 主要结论与发现

1. **不稳定性源于采样策略**，而非迭代生成本身：标准欧拉采样在值梯度BP下产生梯度尖峰和优化崩溃，VINE稳定。
2. **VINE实现稳定端到端优化**：在所有40个OGBench任务中取得最高平均分（60%，次优55%），尤其在长程导航任务（antmaze-giant: 76% vs 次优18%）上大幅领先。
3. **真实世界有效**：VINE在20分钟微调内达到100%成功率，人工干预率仅19.2%，远低于对比方法（55.9%~74.6%）。
4. **迭代生成重要性**：早期步骤动作质量差，增加去噪步骤（K=10）可显著提升成功率（从K=1的0%到K=10的90%）。
5. **理论一致性**：VINE与预训练流策略兼容，无需修改训练目标或引入辅助模型。

## 7. 优点

1. **问题定位准确**：首次明确指出现有方法不稳定性的根源是采样策略而非迭代生成，并给出理论证据。
2. **方法简洁有效**：仅修改采样器实现，无需改变训练目标或模型结构，可直接迁移到预训练策略。
3. **全面保留生成策略优势**：同时保持高表达性（多模态建模）、迭代生成（从粗到细）和端到端值梯度优化。
4. **理论支撑扎实**：提供FM-VINE一致性定理、高斯收缩性分析，证明方法合理。
5. **实验充分且结果显著**：在40个任务全面优于各类基线，真实场景验证迁移性，消融实验解释机制。
6. **开源可复现**：提供视频网站和实现细节，附录包含完整超参数（附录A.4）。

## 8. 不足与局限

1. **算力信息缺失**：未报告GPU型号、数量和训练时长，不利于复现和资源评估。
2. **基线超参数未公开调整细节**：部分对比方法结果可能未经过最优调参，存在潜在不公。
3. **真实世界实验规模有限**：仅测试1个任务（插头插入），且仅有3个方法在同一条件下对比（SAC-Flow、Hil-SERL、VINE）。未在更多真实任务（如抓取、装配）上验证。
4. **离线RL评估仅限成功率**：未报告回报/累计奖励，可能忽略部分性能细节（如路径长度）。
5. **理论分析限于理想条件**：定理1假设最优速度场，推论1假设高斯先验；实际策略可能偏离，需要进一步分析非理想情况下的行为。
6. **仅实验了流匹配策略**：虽然声明可扩展到扩散策略，但未提供实验结果。
7. **行为克隆正则化系数α**：不同任务手动设置（表4），缺乏自适应机制，可能影响泛化。
8. **在线RL设置依赖人类介入**：真实世界实验使用“human-in-the-loop”，干预策略未标准化，结果可能受人工技巧影响。

（完）

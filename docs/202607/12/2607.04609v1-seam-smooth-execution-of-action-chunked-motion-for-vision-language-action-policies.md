---
title: "SEAM: Smooth Execution of Action-Chunked Motion for Vision-Language-Action Policies"
title_zh: SEAM：面向视觉-语言-动作策略的动作分块运动的平滑执行
authors: "Dijia Zhan, Xuemiao Xu, Jinyi Li, Jie Tang"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04609v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 针对视觉-语言-动作政策的免训练推理平滑方法
tldr: "视觉-语言-动作（VLA）策略在固定长度动作块执行时，因独立高斯潜变量导致跨块不一致，产生突发不连续性。SEAM提出一种无需训练的推理时方法，利用前一块未执行尾部作为一致性参考，通过速度引导损失实现闭环矫正，无需反向传播。在LIBERO-10上结合pi_0.5，边界急动减少28%，过渡不连续性降低27%，同时保持基线水平的任务成功率，且去噪循环开销接近无引导基线。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04609v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1831, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04609v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 714, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04609v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 870, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04609v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1835, \"height\": 1278, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04609v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04609v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 701, \"height\": 511, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04609v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 671, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04609v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 539, \"height\": 498, \"label\": \"Table\"}]"
motivation: VLA策略固定长度动作块因独立生成导致相邻块轨迹模式不兼容，引起执行中跳变和边界不连续。
method: SEAM利用前一块未执行尾部作为分析参考，通过速度引导损失在每步欧拉后施加闭式矫正，无需反向传播或重新训练。
result: "在LIBERO-10上，SEAM使边界急动减少28%，块过渡不连续性降低27%，任务成功率与基线持平。"
conclusion: SEAM无需额外训练即可有效平滑动作块运动，保持推理效率，适用于流匹配VLA策略。
---

## 摘要
执行固定长度动作分块的视觉-语言-动作（VLA）策略可能出现多模态分叉：一种跨分块不一致性，其中从独立高斯潜变量生成的相邻分块可能收敛到不兼容的轨迹模式，在分块边界处产生突然的不连续性。现有解决方法要么需要在每个去噪步骤中通过策略进行反向传播，要么依赖于拒绝采样，要么需要重新训练，每种方法都以计算成本或任务可靠性为代价换取更平滑的过渡。我们提出SEAM（动作分块运动的平滑执行），一种针对流匹配VLA的无训练推理时方法。SEAM利用了一个简单的同步执行洞察：在机器人消耗已执行前缀后，前一个分块的未执行尾部已经可以作为分析性一致性参考。其核心机制——速度引导损失调整（VLS）——从该尾部导出一个时间相关目标，并在每个欧拉步后应用闭式校正，无需通过策略网络反向传播。在LIBERO-10上使用pi_0.5，SEAM将边界加加速度减少28%，将分块过渡不连续性降低27%，保持基线水平的任务成功率，并将去噪循环成本保持在接近无引导基线的水平。

## Abstract
Vision-Language-Action (VLA) policies that execute fixed-length action chunks can exhibit multimodal bifurcation: a cross-chunk inconsistency in which adjacent chunks generated from independent Gaussian latents can converge to incompatible trajectory modes, producing abrupt discontinuities at chunk boundaries. Existing remedies either require backpropagation through the policy at each denoising step, rely on rejection sampling, or require retraining, each trading computational cost or task reliability for smoother transitions. We propose SEAM (Smooth Execution of Action-Chunked Motion), a training-free inference-time method for flow matching VLAs. SEAM exploits a simple synchronous-execution insight: after the robot consumes the executed prefix, the previous chunk's unexecuted tail is already available as an analytic consistency reference. Its core mechanism, Velocity-guided Loss Steering (VLS), derives a time-dependent target from this tail and applies a closed-form correction after each Euler step without backpropagating through the policy network. On LIBERO-10 with pi_0.5, SEAM reduces boundary jerk by 28%, reduces chunk transition discontinuity by 27%, preserves baseline-level task success, and keeps denoising-loop cost near the unguided baseline.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在视觉‑语言‑动作（VLA）策略中，动作分块（action chunking）虽可提升局部时间一致性，但由于相邻分块从独立高斯噪声采样生成，两者在重叠区域（即已执行前缀之外的部分）可能收敛到**不兼容的轨迹模式**，导致分块边界处出现突然跳变、急动和不连续，这种现象称为**多模态分叉**。该问题在接触性操作任务中尤为突出，会降低执行可靠性。
- **研究动机**：现有缓解方法（如时序集成、RTC、BID、Legato）要么产生无效中间动作、需要高昂的逐去噪步反向传播、依赖拒绝采样或要求重新训练模型，在**计算成本**与**任务可靠性**之间存在权衡。需要一种**轻量、无训练、推理时**的解决方案。
- **整体含义**：SEAM旨在通过利用异构执行中天然存在的结构（前一分块的未执行尾部）作为分析一致性参考，以极低的额外开销平滑分块边界，且不损害任务成功率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在同步分块执行中，当机器人执行完前一分块的前K个动作后，该分块的**未执行尾部**（长度为 H‑K）已经可用。SEAM将该尾部作为跨分块一致性的“锚点”，在生成下一分块时，通过**速度引导损失调整（VLS）**在去噪ODE的每步欧拉积分后施加闭式校正，引导新分块的前若干重叠动作靠近该锚点，无需反向传播或重训练。
- **关键技术细节**：
  1. **对齐先验构造**：将前一解码分块的未执行尾部 \( a_{\text{tail}} \)（长度 L = H‑K）通过重复最后一个动作扩展为整块长度 H，得到对齐先验 \( a_{\text{al}} \)。
  2. **速度引导损失调整（VLS）**：在每步欧拉候选 \( \tilde{x}_{i+1} \) 计算后，仅对前 M 个重叠动作（M ≤ L）施加损失：
     - 目标：\( r_{i+1} = (1 - t_{i+1}) \cdot a_{\text{al}}[1:M] \)
     - 损失：\( \ell_{i+1} = \| \tilde{x}_{i+1}[1:M] - r_{i+1} \|_2^2 \)
     - 闭式梯度：\( g_{i+1} = -2(\tilde{x}_{i+1}[1:M] - r_{i+1}) \)
     - 校正更新：\( x_{i+1}[1:M] = \tilde{x}_{i+1}[1:M] + \lambda (1 - t_{i+1}) \cdot g_{i+1} \)
  3. **时序衰减**：系数 \( \lambda(1 - t) \) 使早期噪声状态受弱引导，而潜变量接近动作流形时增强校正。
- **算法流程（文字说明）**：
  - 输入：观测 o、前一尾部 \( a_{\text{tail}} \)、速度场 \( v_\theta \)、超参数 H、K、M、λ、ODE步数 N。
  - 构建对齐先验 \( a_{\text{al}} \)。
  - 初始化潜变量 \( x_0 \sim \mathcal{N}(0,I)^{H\times D} \)；设置时间步 \( t_i = 1 - i/N \)。
  - 对于 i = 0 至 N‑1：
    - 计算标准欧拉候选 \( \tilde{x}_{i+1} = x_i + (t_{i+1} - t_i) v_\theta(x_i, t_i, o) \)。
    - 计算重叠目标 \( r_{i+1} \)。
    - 应用 VLS 校正（闭式）到 \( \tilde{x}_{i+1}[1:M] \)。
    - 保持未引导位置不变。
  - 解码最终 \( x_N \) 并执行前 K 个动作。
- **计算开销**：仅需 N·M·Dg 次标量运算，无反向传播，去噪循环成本接近基线。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集与场景**：**LIBERO-10**，包含10项长时间跨度的桌面操作任务（如抓取、放置、开箱等），在 LIBERO 模拟环境中执行。每项任务均需多步骤推理。
- **基准策略 Backbone**：**π₀.5**，基于 PaliGemma‑2B 视觉语言模型、SigLIP 视觉编码器和 Gemma‑300M 动作专家。动作分块大小 H=50，ODE 步数 N=10，每步执行 K=10，重叠 L=40。
- **对比方法**：
  - **基线 π₀.5**（无任何平滑处理）。
  - **ACT 时序集成（ACT‑TE）**：标准系数 k=0.01，每环境步查询策略。
  - **RTC**：采用 ΠGDM 引导（最大权重 β=5，指数前缀注意力衰减），需反向传播。
- **评估指标**：
  - **任务成功率**（%）。
  - **运动质量指标**（计算执行后动作序列）：边界加加速度 BJ、内部加加速度 IJ、分块过渡不连续性 CD、边界加速度方差 AV_b。
  - **去噪循环时间成本**：每分块毫秒数及相对基线倍率 D‑Cost。
- **消融实验**：对 VLS 强度 λ（0.05–0.2）、引导维度（全维度 vs 仅位置）、引导窗口长度 M（2–20）进行系统扫描。

### 4. 资源与算力

- **未明确说明训练算力**：SEAM 是**推理时方法**，无需训练，因此论文未提及 GPU 数量或训练时长。仅报告了推理阶段的时间测量：
  - 硬件：单个 **RTX 3090**。
  - 去噪循环耗时（每分块 10 步）：
    - 基线 π₀.5：282.2 ms
    - SEAM：286.0 ms（**1.01×** 基线）
    - RTC：344.6 ms（1.22× 基线）
  - 未提供其他骨架或分布式推理的算力信息。

### 5. 实验数量与充分性

- **主要对比实验**：1张主表（Table 2）涵盖基线 + 3种方法在 LIBERO-10 的10项任务，每项任务 130 episodes，统计量较大。
- **消融实验**：2张消融表（Table 4、5）分别覆盖 λ（5个值）和 M（10个值）的扫描，以及维度选择（全维度 vs 位置维度）。
- **定性分析**：针对任务 T1 的失败模式可视化（图4），解释了为什么该任务上不同方法表现差异最大。
- **充分性评价**：实验设计较为**充分**：
  - 覆盖了核心超参数（λ、M、引导维度）的影响。
  - 评估指标兼顾任务成功率与运动平滑性，并单独测量了计算成本。
  - 对比方法均为当前代表性方法（RTC、ACT‑TE），且使用它们官方推荐的设置，确保**公平**。
  - 在模拟环境 LEBERO-10 上的测试规模合理，但**缺乏真实机器人验证**或更多多样的任务域，可能限制了泛化性结论。

### 6. 论文的主要结论与发现

- **主要结论**：SEAM 是一种**有效的、无训练、推理时**平滑方法，在保持基线任务成功率（95.7% vs 94.8%）的前提下：
  - 降低边界加加速度（BJ）**28%**（0.195→0.141）。
  - 降低分块过渡不连续性（CD）**27%**（0.172→0.126）。
  - 降低内部加加速度（IJ）21.3%，以及边界加速度方差（AV_b）43.0%。
  - 去噪循环成本仅增加 **1%**（1.01×基线）。
- **与现有方法对比**：
  - **RTC** 可获得更低的 BJ 和 CD，但去噪循环成本高出 22%，且需反向传播。
  - **ACT‑TE** 可取得最高的平滑度，但任务成功率大幅下降至 82.7%。
  - SEAM 在平滑性、任务保持和计算开销之间取得了**更好的平衡**。
- **消融发现**：引导强度 λ=0.1、全维度、窗口长度 M=20 提供最佳折中；仅位置引导效果较弱；窗口长度主要影响平滑度，不显著影响成功率。

### 7. 优点：方法或实验设计上的亮点

- **方法学亮点**：
  - **真正无训练、即插即用**：无需修改模型架构或重新训练，直接应用于训练好的流匹配 VLA。
  - **极低计算开销**：闭式梯度计算免于后向传播，仅增加少量标量运算，适用于实时或边缘部署。
  - **理论简洁**：直观地利用同步执行中天然存在的未执行尾部，构造时间依赖的分析目标。
  - **可调节性**：通过 λ 和 M 两个参数可在平滑强度和策略自由度之间灵活权衡。
- **实验设计亮点**：
  - 不仅报告任务成功率，还使用多维度运动质量指标（BJ、IJ、CD、AV_b）定量刻画平滑度。
  - 单独测量并对比“去噪循环时间成本”，使读者能清晰评估实际部署开销。
  - 消融实验系统全面，覆盖关键超参数及引导维度选择。
  - 定量结果附带定性失败模式分析（T1任务），直观展示了不同方法的行为差异。

### 8. 不足与局限

- **实验覆盖不够广**：
  - 仅在一个模拟环境（LIBERO-10）上评估，未在真实机器人平台验证，缺乏对真实世界噪声、动态的测试。
  - 仅使用 π₀.5 作为骨干，未验证对其他流匹配或扩散 VLA（如 OpenVLA、Octo）的通用性。
- **可能的风险与偏差**：
  - 虽然总体成功率持平，但在某些任务（如 T7–T10）上 SEAM 略低于 RTC 或基线，说明存在**任务级别的不一致**，可能对失败模式不够鲁棒。
  - 超参数 λ 和 M 仍需手动调优，未提出自适应选择机制；不同骨干或任务可能需要重新调整。
  - 仅考虑了重叠区域的前 M 个动作，无法保证后续动作的全局一致性（但论文实验未暴露明显缺陷）。
- **应用限制**：
  - 假设执行是同步的（执行 K 个动作后立即请求下一分块），对于异步执行或可变执行长度可能不适用。
  - 依赖于前一分的未执行尾部作为“锚点”，若前一分块本身存在误差或噪声，尾部可能不是完美参考，可能导致错误累积。
  - 未讨论与动作维度缩放、多机器人协同等扩展场景的兼容性。
- **缺失信息**：论文未披露 π₀.5 在 LIBERO 上的训练算力需求（GPU、时长），也未与其他近年方法（如 BID、Legato）进行直接的实验对比，这些方法虽有前提条件但与 SEAM 思路不同，缺少定量比较。

（完）

---
title: Temporal Logic Guidance for Action-Only Diffusion Policies with World Models
title_zh: 基于世界模型的动作扩散策略的时态逻辑引导
authors: "Moritz Zoellner, Anastasios Manganaris, Rohan Paleja"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22729v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 基于时序逻辑的动作扩散策略引导
tldr: "扩散策略在推理时难以选择行为模式，现有STL指导方法需同时生成动作和状态，增加复杂度。本文提出利用单独学习的世界模型对STL鲁棒性进行可微评估，将梯度注入仅动作扩散策略实现约束引导。在Can Transport任务中保持100%成功率，约束违反从80%降至4%。该方法无需重新训练，降低了复杂性，并支持扩展至更复杂约束。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22729v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22729v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 926, \"height\": 631, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22729v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 161, \"label\": \"Table\"}]"
motivation: 解决扩散策略推理时无法控制行为模式的问题，避免现有STL方法需联合生成动作和状态的高复杂性。
method: 使用单独学习的世界模型实现STL鲁棒性的可微评估，将梯度注入仅动作扩散过程以引导行为满足约束。
result: "在Can Transport任务中保持100%任务成功率，约束违反从基线方法的80%以上降低至4%。"
conclusion: 提出一种轻量级方法，在不重训练条件下提升扩散策略的约束遵守能力，可扩展至更复杂约束和鲁棒性改进。
---

## 摘要
扩散策略能够实现多模态机器人行为，但在推理时选择行为模式的能力有限，尽管在人机交互场景中这种控制是可取的。先前针对这种控制缺失的解决方案利用信号时态逻辑（STL）来表达人类意图，并为扩散策略推理提供相应的引导。然而，这些方法只能引导同时生成未来动作和状态的扩散策略，增加了复杂性和运行时开销。我们提出了一种针对动作扩散策略的新颖引导方法，该方法使用一个单独学习的世界模型来实现STL鲁棒性的可微评估，并将其梯度注入扩散过程。这在不重新训练的情况下将行为引导向约束满足，提高了约束遵守程度同时保持任务性能。在Robomimic的Can Transport任务中，我们的方法保持了100%的任务成功率，同时将约束违反率从基线方法的超过80%降低到4%。我们还讨论了向更鲁棒和更复杂约束的扩展。

## Abstract
Diffusion policies enable multimodal robot behavior but offer limited ability to choose among behavior modes at inference time, even though such control is desirable in human-robot settings. Prior solutions to this lack of control have utilized Signal Temporal Logic (STL) to express human intentions and provide corresponding guidance for diffusion policy inference. However, these approaches can only guide diffusion policies that jointly generate future actions and states, increasing both complexity and runtime. We propose a novel guidance method for action-only diffusion policies that uses a separate learned world model to enable differentiable evaluation of STL robustness, with its gradient then injected into the diffusion process. This steers behavior toward constraint satisfaction without retraining, improving constraint adherence while preserving task performance. On the Can Transport task from Robomimic, our method maintains 100% task success while reducing constraint violations from over 80% for baseline methods to 4%. We also discuss extensions toward improved robustness and more complex constraints.

---

## 论文详细总结（自动生成）

# 论文总结：基于世界模型的动作扩散策略的时态逻辑引导

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：扩散策略能够在推理时生成多模态的机器人行为，但用户无法在推理时控制或选择特定行为模式，例如在洗碗任务中偏好“小心放置”而非“紧密堆叠”。现有基于信号时态逻辑（STL）的引导方法要求扩散策略同时生成动作和状态序列，这增加了模型的复杂性和推理时长，且不适用于仅输出动作序列的主流扩散策略。
- **研究动机**：在不重新训练模型的前提下，利用用户以STL表达的偏好（如“保持罐子直立”）来引导策略行为，同时保持任务成功率。
- **整体意义**：提出一种轻量级、推理时引导方法，使仅动作扩散策略能通过单独训练的世界模型实现STL约束的满足，从而提升人机交互中的适应性。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：使用一个单独学习的可微世界模型来预测动作序列导致的未来状态，然后根据STL鲁棒性度量计算引导目标，并将其梯度注入扩散去噪过程，以引导采样行为满足约束。
- **关键技术细节**：
  - **扩散策略**：π(a_{t:t+H} | s_t) 生成动作序列，仅包含动作，无状态预测。
  - **推理时引导**：在每一步去噪中，修改原更新公式，加入引导目标 J 的梯度：
    a_{k-1} = μ_θ(a_k, s_t, k) + λ ∇_{a_k} J(ϕ, a_k, s_t) + σ_k ε
  - **引导目标 J**：基于STL规范 ϕ 的鲁棒性 ρ_ϕ(ˆs_{t+1:t+H})，其中 ˆs 是世界模型 f_θ(s_t, a_t) 迭代预测的未来状态轨迹。
  - **世界模型**：单独训练，从相同数据集中学习，可微，使得梯度能够由状态轨迹反向传播到动作序列。
  - **额外步骤**：在获得最终动作序列 a_0 后，额外进行少量梯度上升步骤以增强约束满足。
- **公式**（文字说明）：标准去噪预测 → 加上带权重 λ 的引导梯度 → 再加噪声。所有步骤可微，保证端到端引导。

## 3. 实验设计

- **数据集/场景**：Robomimic 中的 Can Transport 任务——机器人抓取罐子并放入盒子。
- **Benchmark**：基础扩散策略（无引导）以及一个“采样与排序”基线方法（Sample & Rank），该基线使用相同的鲁棒性目标对多次采样结果进行排序选择。
- **对比方法**：
  - 基础策略（Base Policy）
  - 采样与排序（Sample & Rank）
  - 本文方法（Guidance，Ours）
- **约束**：STL 规范 G(R_{zz} > cos(5°))，即要求罐子倾斜角度始终保持在5度以内（直立约束）。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长等具体算力信息。仅在摘要中提到方法的复杂度降低，但无量化指标。未来工作中也未涉及算力细节。

## 5. 实验数量与充分性

- **实验数量**：仅在一个任务（Can Transport）上进行了50次 rollout 的评估（表I）。未涉及其他任务、环境或更复杂的STL规范。
- **充分性**：实验覆盖有限。虽然结果显示约束违反率从84%降至4%，且任务成功率保持100%，但缺乏多任务、多约束、多随机种子、对世界模型误差的鲁棒性分析等。文中也承认当前实验是初步的，未来需评估更多场景。
- **公平性**：与基线的对比合理，但采样与排序方法未调整参数，且梯度引导的λ设置可能经过调优，但未提及超参数搜索细节。总体而言，实验客观性尚可，但充分性不足。

## 6. 主要结论与发现

- 本文方法在保持100%任务成功率的同时，将约束违反率从基线的84%降低至4%，远优于采样与排序方法（82%违反率）。
- 梯度引导比基于采样的选择更有效地将策略导向约束满足行为，尤其能恢复数据分布中未被充分代表的模式。
- 世界模型能够准确预测多步 rollout（图2a），为引导提供了可靠的状态轨迹。

## 7. 优点

- **方法创新**：首次将世界模型与仅动作扩散策略结合实现STL引导，降低了对策略输出状态的依赖，通用性强。
- **无需重新训练**：只需在推理时加入世界模型和梯度计算，适用于现有的大部分扩散策略。
- **可扩展性**：未来可通过学习自动机状态预测来支持长时域约束，且任务成功率和约束满足同时保持。
- **实验指标清晰**：报告了平均倾斜角度、成功率、违反率三个关键指标，直观反映性能。

## 8. 不足与局限

- **实验覆盖不足**：仅评测一个任务，缺乏在多种任务、不同STL规范、不同环境配置下的验证。
- **世界模型误差风险**：世界模型的预测误差可能累积，影响引导效果，文中未分析鲁棒性。
- **超参数敏感**：引导强度λ的选择可能对性能有显著影响，但未讨论调参过程或敏感性分析。
- **扩展方向未验证**：提出的长时域约束扩展思路（自动机状态预测）仅作为未来工作，尚未实现。
- **计算资源未报告**：缺乏算力需求评估，难以判断实际部署成本。
- **用户交互部分未涉及**：虽提到自然语言到STL的映射，但实验中直接使用手工STL规范，未验证端到端用户交互流程。

（完）
